# “桩”点北邮 · 充电桩车位防占系统 — 第 0 天开发文档

> 目的：在三人分别开工前，把**总体方案、协作契约、分工任务**一次性对齐并落进仓库，
> 保证后续并行开发互不阻塞、不返工。
> 适用对象：吕浩（统筹/集成）、汤瑾睿（后端/DB/算法）、吴和庆（UI/美术）。
> 配套规范：本仓库 `AGENTS.md`（每次改动必 commit、交付前测试全过）。

---

## 1. 项目一句话与 MVP 定义

- **一句话**：用"计算机视觉 + 充电桩状态"融合，自动识别燃油车占位 / 新能源充满未移车，并主动提醒车主，形成"识别—判断—提醒"智能闭环。
- **形态**：演示系统原型（校园场景），完整跑通业务闭环。
- **技术栈**：FastAPI(Python) + Vue3/Vite/Element Plus + MySQL 8 + YOLOv8(车辆检测) + HyperLPR/PaddleOCR(车牌)；充电状态用**模拟数据**（预留 OCPP 适配层）。
- **MVP 第一步（本周目标）**：先跑通一条**端到端最小闭环**——
  `输入帧 → 识别桩(替身) → 规则引擎 → 短信沙箱(模拟) → 后台页面`，
  其中"识别"用桩替身返回标注车牌、"短信"用沙箱写库+日志，**CV 模型与真实短信后续替换**。

## 2. 总体原则（总工视角）

1. **契约先行**：`CONTRACT.md` 是唯一事实源，数据模型 / 接口 / 类型写死，三人不脑补对方接口。
2. **桩替身解耦风险**：最慢最风险的 CV 模型（YOLO+HyperLPR）先用"识别桩 stub"顶上；短信先用沙箱。先证明闭环能跑，再换真东西。
3. **独立可测再集成**：每个模块自带测试（汤 pytest、吕 TestClient、吴设计评审），集成只是"接线"，不是联合调试。
4. **短周期合并**：基于 `main` 开 `feature/<name>`，一两天一合；吕浩为集成负责人统一合入，避免各合各的弄乱 `main`。

## 3. 仓库结构（第 0 天落地）

```
充电桩/
├── backend/
│   ├── app/
│   │   ├── main.py            # FastAPI 入口 + /api/health
│   │   ├── schemas.py         # Pydantic：RecognitionResult / ViolationRecord / NotifyReq / NotifyResp
│   │   ├── routers/           # API 路由占位
│   │   └── rule_engine.py     # 规则引擎（本周实现）
│   ├── sql/
│   │   └── schema.sql         # 四张表建表 + 种子数据
│   └── tests/                 # pytest / TestClient 用例
├── frontend/                  # Vue3 项目（后续）
├── algo/                      # YOLO / HyperLPR 训练与适配（后续）
├── docs/
│   ├── design/                # 设计系统、线框、切图（吴）
│   └── CONTRACT.md            # 接口/数据契约（唯一事实源）
├── DAY0_PLAN.md               # 本文档
├── .gitignore
└── AGENTS.md                  # 协作纪律
```

## 4. 共同任务（第 0 天上午，三人一起，约半日）

1. 通读两份文档（开发大纲 + 立项申请书），对齐范围与 MVP 定义。
2. **逐条确认 `CONTRACT.md`**（见第 6 节）：四表字段、`RecognitionResult`、`ViolationRecord`、4 个 API 端点、`system_config` 配置项。
3. 约定开发基线：Python 3.11+、Node LTS、MySQL 8 版本统一。
4. 约定仓库结构、分支策略、文件归属、lint/commit 规范（见第 7 节）。
5. 写下"第 1 周末 Demo 验收清单"（见第 9 节）。

## 5. 分工任务（第 0 天下午，三人并行）

### 5.1 吕浩（统筹 / 集成负责人）
1. 建目录骨架 + `.gitignore`（忽略 `.venv`、`node_modules`、`__pycache__`、`.env`、`.workbuddy/`）。
2. **主导写 `CONTRACT.md`** 并提交（三人已确认）。
3. 搭 FastAPI 骨架：`main.py` + `schemas.py`（Pydantic 模型）+ `/api/health` 路由——把契约落成代码，自动出 OpenAPI。
4. 配 `pyproject.toml`：`ruff` + `black`；前端 `eslint` + `prettier`（可先占位）。
5. 写分支/协作说明（进 `CONTRACT.md` 或 `DEV.md`）：`main` + `feature/<name>`，吕浩统一合入。
6. 写冒烟测试 `tests/test_health.py`（TestClient `GET /api/health` == 200）。
7. 全部 commit + push，`@全员`"脚手架就绪，可开局"。

### 5.2 汤瑾睿（后端 / DB / 算法）
1. 通读 `CONTRACT.md`，确认四表与 `RecognitionResult` 够规则引擎用，疑问当场提。
2. 写 `backend/sql/schema.sql`：四表建表 + **种子数据**（新能源/燃油各若干、桩状态样例、超时阈值默认值）。
3. 本地 MySQL 8 跑通 `schema.sql`，确认建表 + 插种无报错。
4. 起草 `rule_engine.py` 与**四规则单测骨架** `tests/test_rule_engine.py`（用例：燃油占位/异常占位/充满未移车/正常不提醒；函数体先占位，本周实现）——对齐"输入什么、输出什么"。
5. 定识别桩数据格式：准备 1–2 张测试图 + 同目录 JSON（字段与 `RecognitionResult` 完全一致），供联调。
6. commit `schema.sql` + 单测骨架 + 标注样例。

### 5.3 吴和庆（UI / 美术）
1. 通读 `CONTRACT.md` 的"后台 6 页"范围，确认页面清单与字段。
2. 建设计系统基线 `docs/design/design-tokens.md`：主色、状态色（违规红/正常绿/警告黄）、字体、圆角、间距，对齐 Element Plus 主题。
3. 列后台组件清单（对应 Element Plus：表格/表单/卡片/统计数/弹窗）。
4. 产出第 1 页"车辆信息管理"线框，作风格样板与吕浩对齐（表格列 = `vehicle` 字段）。
5. 定切图规范：格式（SVG 优先）、命名（kebab-case）、存放 `docs/design/assets/`。
6. 把 tokens + 线框 + 规范推到 `docs/design/`（或 Figma 链接写进 `CONTRACT.md`），供吕浩引用。

## 6. 契约草案（CONTRACT.md 内容，待三人确认）

### 6.1 数据模型（核心四表）

**vehicle（车辆信息）**
| 字段 | 类型 | 说明 |
|---|---|---|
| plate | VARCHAR(15) PK | 车牌号 |
| vtype | ENUM('新能源','燃油') | 车型（由绿牌/蓝牌判定） |
| owner | VARCHAR(50) | 车主 |
| phone | VARCHAR(20) | 绑定手机号 |
| created_at | DATETIME | 录入时间 |

**charging_pile（充电桩状态，模拟）**
| 字段 | 类型 | 说明 |
|---|---|---|
| pile_id | VARCHAR(20) PK | 桩 ID |
| status | ENUM('空闲','充电中','已充满') | 运行状态 |
| bound_plate | VARCHAR(15) | 绑定车牌（FK vehicle.plate） |
| start_time | DATETIME | 开始充电时间 |
| end_time | DATETIME | 充满时间 |

**occupation_record（违规记录）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK AUTO_INCREMENT | 记录 ID |
| plate | VARCHAR(15) | 车牌 |
| vtype | ENUM('新能源','燃油') | 车型 |
| rule_hit | TINYINT | 1 燃油占位 / 2 异常占位 / 3 充满未移车 / 0 正常 |
| occur_time | DATETIME | 命中时间 |
| notify_status | ENUM('未提醒','已提醒','失败') | 提醒状态 |
| notify_time | DATETIME | 提醒时间 |

**system_config（系统参数）**
| 字段 | 类型 | 说明 |
|---|---|---|
| key | VARCHAR(30) PK | 参数键 |
| value | VARCHAR(50) | 参数值 |
| note | VARCHAR(100) | 说明 |

默认值：`full_timeout_min=30`（充满超时阈值）、`abnormal_park_min=30`（异常占位久停阈值）。

### 6.2 识别结果 `RecognitionResult`
```json
{
  "plate": "京AD12345",
  "vtype": "新能源",
  "confidence": 0.97,
  "bbox": [120, 80, 200, 120],
  "frame_time": "2026-09-08T10:00:00"
}
```
> 注：真实 CV 与识别桩 stub **返回同一结构**，汤后续换模型时吕浩前端零改动。

### 6.3 违规记录 `ViolationRecord`（API 返回 / 对应 occupation_record）
```json
{
  "plate": "京A12345",
  "vtype": "燃油",
  "rule_hit": 1,
  "occur_time": "2026-09-08T10:00:05",
  "notify_status": "未提醒"
}
```

### 6.4 API 端点（吕浩定义，Pydantic 校验）
| 方法 | 路径 | 说明 | 请求 → 响应 |
|---|---|---|---|
| POST | `/api/recognize` | 输入帧 → 识别结果（stub 读标注） | frame_ref → `RecognitionResult` |
| POST | `/api/judge` | 识别结果 → 违规判定（引擎内部查桩状态） | `RecognitionResult` → `ViolationRecord`\|null |
| POST | `/api/notify` | 违规 → 短信沙箱（写库+日志） | `ViolationRecord` → `{notify_status}` |
| GET | `/api/records` | 后台查询违规记录 | `?page=&size=` → `[ViolationRecord]` |
| GET | `/api/health` | 冒烟/健康检查 | → `{status:"ok"}` |

### 6.5 系统配置项
`full_timeout_min`（充满超时阈值）、`abnormal_park_min`（异常占位久停阈值），均来自 `system_config`，禁止硬编码。

## 7. 协作与工程规范

- **分支**：`main` 为保护分支；功能开 `feature/<name>`（如 `feature/rule-engine`），吕浩统一合入。
- **文件归属**：`backend/app/**`、`sql/**` → 汤/吕；`frontend/**` → 吕（实现）+ 吴（设计）；`algo/**` → 汤；`docs/design/**` → 吴。
- **commit 规范**：参考 `AGENTS.md`，每完成一步即提交，信息用 `模块: 简述`（如 `db: 新增四表 schema 与种子`）。
- **lint/format**：Python `ruff`+`black`；前端 `eslint`+`prettier`，提交前自动格式化，减少无意义 diff 冲突。
- **改契约流程**：任何结构变动 → 先改 `CONTRACT.md` → **@全员 + 升版本号**，禁止偷偷改自己那侧。

## 8. 散会 Checkpoint（必须全勾）

- [ ] `CONTRACT.md` 已合入 `main`，三人确认
- [ ] `schema.sql` 已合入，表能建
- [ ] FastAPI 骨架本地起得来，`/api/health`=200，冒烟测试过
- [ ] 三人各用 1 分钟讲"我下周**提供/调用**什么"，互相确认对得上
- [ ] 第 1 周末 Demo 验收清单已写入 `CONTRACT.md`

## 9. 第 1 周末 Demo 验收标准

端到端跑通一条可演示的最小闭环：
**输入一张测试帧 → 识别桩返回车牌+类型 → 规则引擎判定违规 → 短信沙箱写库+日志 → 后台页面能看到这条违规记录。**

- 本周暂不卡准确率/响应时间指标（CV 为桩替身、短信为沙箱），**闭环优先**。
- 每人自带测试全过后再集成（汤 pytest 覆盖四规则、吕 TestClient 测端点与沙箱）。

## 10. 与 AGENTS.md 对齐

- 每次改动完成后必须创建一个对应 Git commit（便于追赃与回滚）。
- 每次改动后必须编写/更新相关测试，交付用户前确保所有测试与验证全部通过。

## 附录 A：建议第 0 天直接落地的脚手架文件清单

| 文件 | 负责人 | 说明 |
|---|---|---|
| `CONTRACT.md` | 吕浩 | 接口/数据契约（第 6 节） |
| `backend/app/main.py` + `schemas.py` | 吕浩 | FastAPI 骨架 + Pydantic 模型 |
| `backend/sql/schema.sql` | 汤瑾睿 | 四表建表 + 种子 |
| `backend/app/rule_engine.py` + `tests/test_rule_engine.py` | 汤瑾睿 | 规则引擎签名 + 四规则单测骨架 |
| `backend/tests/test_health.py` | 吕浩 | 冒烟测试 |
| `docs/design/design-tokens.md` + 首 page 线框 | 吴和庆 | 设计系统基线 |
| `.gitignore` / `pyproject.toml` | 吕浩 | 忽略项与 lint 配置 |
