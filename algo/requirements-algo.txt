# CV / 算法环境依赖（任务 2.1 交付物）

# ⚠️ 前提：torch 与 torchvision **不要用本文件安装**。
# Windows 上 PyPI 的 torch wheel 是 CPU 版，必须先单独执行（CUDA 12.4 版）：
#   pip install --index-url https://download.pytorch.org/whl/cu124 torch torchvision
# 装完再执行：pip install -r algo/requirements-algo.txt

ultralytics==8.4.155      # YOLOv8（车辆/车牌检测）
hyperlpr3==0.1.3          # 车牌识别（绿牌/蓝牌判定）
opencv-python==5.0.0.93   # 读图与预处理
onnxruntime==1.30.0       # HyperLPR3 推理后端
numpy==2.4.6              # 与 opencv 5.x / onnxruntime 1.30 对齐
pillow==12.3.0            # 合成数据集生成
