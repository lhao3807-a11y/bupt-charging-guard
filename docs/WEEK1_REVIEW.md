# 吕浩 · 第 1 周完成情况审查

> **审查日期**：2026-09-19
> **审查对象**：`PLAN_4WEEKS.md` §2.1（任务 1.1–1.10）+ 契约 §11 验收清单
> **审查方式**：**不采信自评文档，全部复跑复现**。三项硬门禁 + 前端三验证 + 真实 HTTP e2e + 逐文件字节比对。
> **契约版本**：`docs/CONTRACT.md` **v1.4**

## 一、结论

**吕浩本人任务 8/10（1.9、1.10 曾未完成，现已补齐）；契约 §11 验收 8/10。**

> ⚠️ **口径更正**：本报告初版写「§11 验收 6/8」，系**口径错误**——
> 拿的是 `PLAN_4WEEKS.md` §2.4 的**旧 8 项表**（该表写于契约 v1.2 时期）。
> 契约 v1.3 新增两条验收（筛选参数、车辆 CRUD）后，**§11 实际是 10 项**，PLAN §2.4 未同步。
> 正确口径为 **8 ✅ / 2 ⏳ = 8/10**。已在下文 §四修正。

| 维度 | 结果 |
|---|---|
| PLAN 任务 1.1–1.10（吕浩） | **10 ✅**（1.9、1.10 本轮补齐） |
| 契约 §11 验收 **10 项** | **8 ✅ / 2 ⏳**（筛选参数、车辆 CRUD —— **均卡在汤瑾睿**） |
| 三项硬门禁 | **全绿** |
| 前端三验证 | **全绿** |
| e2e 闭环 | **12/12 PASS**（后端 8 + 前端 4） |

> **判据**：第 1 周的核心是契约 §11「后台能看到违规记录」。
> 该项**已用真实浏览器截图证明达成**（`docs/acceptance/week1/p2-records.png`），
> 故本周**主线目标成立**，可进入第 2 周。
>
> ⚠️ **但「进入第 2 周」有个前置条件被本报告初版低估了**：见 §五·P0-3，
> **汤瑾睿的第 1 周任务（含最高风险的 CV 环境）实际上一项都没开工**，
> 而第 2 周的关键路径正是 CV 模型。这是全项目当前最大的风险，已单列。

---

## 一·补、那 2 项验收为什么没完成（根因）

用户的疑问是切中要害的：**为什么"6/8"还能算完成？**

排查后确认这是**三层责任混淆**，必须拆开看：

### 第 1 层：口径错误（本报告的问题，已更正）

`PLAN_4WEEKS.md` §2.4 的验收表是 **8 项**，写于契约 v1.2 时期。
契约 v1.3（2026-09-19）新增两条验收后，**§11 变成 10 项**，但 **PLAN §2.4 没同步**。
本报告初版直接引用 PLAN 的旧表，导致「6/8」这个数字**分母错了**。正确是 **8/10**。

**这暴露一个纪律缺口**：契约升版时，只检查了「代码注释是否过时」（P0-1），
**没检查「引用契约的其他文档是否过时」**。PLAN §2.4 就是漏网的一份。

### 第 2 层：计划断层（真正的问题）

**契约加的那两条验收，在 PLAN 的任务清单里根本没有对应任务。**

- 契约 §11 第 5 条「筛选参数生效」→ 对照 PLAN §2.1 吕浩任务 1.1–1.10：**无对应项**
  （1.6 只写"筛选 + 表格 + 分页"，没写"服务端筛选"）
- 契约 §11 第 7 条「车辆 CRUD」→ 对照 PLAN §2.1 / §2.2：**无对应项**

也就是说：**§11 加了验收，但没人被派活去做。**
汤瑾睿按 PLAN §2.2（2.1–2.7）干活，那 7 项里没有任何一条提到这两个接口；
吕浩按 PLAN §2.1 干活，1.6/1.7 只要求"页面可用"。

**这是我在 v1.3 改契约时的疏漏**——加了验收项，却没同步更新任务分派。
正确的做法应当是：改 §11 的同时，在 PLAN 里给这两条各自指定责任人。

### 第 3 层：责任人是谁（结论）

| 契约 §11 项 | 性质 | 责任 | 依据 |
|---|---|---|---|
| 第 5 条 筛选参数（§6.4） | 后端接口 | **汤瑾睿** | 契约 §9 归属：`backend/app/**` → 汤/吕；此接口属后端 |
| 第 7 条 车辆 CRUD（§6.6） | 后端接口 | **汤瑾睿** | 同上 |

前端侧已就绪并留好替换点（`stores/{records,vehicle}.ts` + `README.md` §6），
**后端一实现，前端切换是小改动**。故这 2 项的瓶颈**完全在后端**。

> **给汤瑾睿的任务书已经存在**：`docs/TASK_TANG_v1.3.md`（含 curl 验收命令与
> 「删除不级联」专项测试要求）。所以这两项**不是"没人做"，而是"活没派到位"** ——
> 任务书写了，但 PLAN 的任务清单没更新，汤瑾睿的第 1 周计划里看不到这两条。

---

## 二、复跑证据（全部实跑，非引用）

| # | 验证项 | 命令 | 实测结果 |
|---|---|---|---|
| 1 | 后端测试 | `pytest backend/tests -q` | **57 passed**，1 warning |
| 2 | Python lint | `ruff check backend scripts` | **All checks passed**（exit 0） |
| 3 | 设计令牌校验 | `check_tokens.py` | **exit 0**，5 页漂移 0 / 裸色值 0 |
| 4 | 反例自测 | `selftest_check_tokens.py` | **13/13 全部拦下**，还原后 exit 0 |
| 5 | 前端类型 | `vue-tsc --noEmit` | **exit 0**，零错误 |
| 6 | 前端构建 | `vite build` | **成功**，6 页 code-split |
| 7 | 前端 lint | `eslint .` | **exit 0**，0 错 0 警 |
| 8 | 端到端闭环 | `scripts/e2e_check.py` | **8/8 PASS**，`ALL PASSED` |
| 9 | 前端裸色值 | grep `#[0-9A-Fa-f]{3,8}` | **0 处** |
| 10 | 令牌漂移 | `diff docs/design/tokens.css frontend/...` | **逐条一致，零漂移** |

**反向验证（关键）**：契约 §6.4 的筛选参数**实测无效**——
`GET /api/records?rule_hit=3` 返回了 `rule_hit=1` 的记录，`total` 也未随筛选变化。
后端**忽略了全部筛选参数**，说明汤瑾睿尚未开工（符合预期，任务书 `TASK_TANG_v1.3.md` 已发）。

---

## 三、逐任务对照

| # | 任务 | 判定 | 证据 / 缺口 |
|---|---|---|---|
| 1.1 | 前端脚手架 | ✅ | Vite 6 + Vue 3.5 + TS 5.6 + EP 2.8 + Router + Pinia + Axios，可 dev/build |
| 1.2 | 接入设计令牌 | ✅ | `src/styles/tokens.css` 存在（是我初查漏看），`main.ts` 顺序 tokens → EP → global 正确；裸色值 0 |
| 1.3 | AppShell + 通用件 | ✅ | 超预期：11 件（9 规格件 + `PlateTag`/`RuleHitPill`/`NotifyPill` 3 领域件） |
| 1.4 | 导航 6 项（第 6 置灰） | ✅ | `navRoutes` 顺序 = 契约 §1.1；`reserved` → `:disabled` + 「预留」角标 |
| 1.5 | API 客户端 + 代理 | ✅ | 5 端点封装；proxy → `127.0.0.1:8000` 实测通 |
| 1.6 | **第 2 页** ★ | ⚠️ **功能达成，口径未切** | 页面可用、真实数据渲染（截图存证）；但筛选仍是**前端过滤当前页**，未走 §6.4 参数 |
| 1.7 | 第 1 页 | ⚠️ **UI 达成，数据源临时** | 全字段列 + 弹窗 + 二次确认齐备；但用 **localStorage**，未接 §6.6 |
| 1.8 | 第 3/4/5 页占位 | ✅ | `PlaceholderView` 标出「开发中」+ 计划周次 + 依赖缺口，无白屏 |
| 1.9 | **联调与 e2e 回归** | ⏳ **未完成** | `e2e_check.py` 仍是**纯后端 API**，未扩到前端；前端 typecheck/lint/build 虽过，但不等于联调 |
| 1.10 | 本周集成与合入 | ⏳ **部分** | 4 个提交已 push，`main` 干净；但 1.9 未完 → 本任务未达标 |

---

## 四、契约 §11 验收 8 项

| # | 验收点（契约 §11 现有 **10 条**） | 判定 | 责任人 | 说明 |
|---|---|---|---|---|
| 1 | `POST /api/recognize` 返回合规结果 | ✅ | 汤/吕 | e2e PASS |
| 2 | `POST /api/judge` 四规则正确 | ✅ | 汤 | 实测规则④ 返回 `null`，规则①/③ 命中 |
| 3 | `POST /api/notify` 沙箱写库 + 日志 | ✅ | 吕 | e2e PASS |
| 4 | `GET /api/records` 分页 `total` 正确 | ✅ | 吕 | 全表分页正确 |
| 5 | **`GET /api/records` 筛选参数（§6.4）生效** | ⏳ | **汤** | **后端未实现**：`?rule_hit=3` 仍返回 `rule_hit=1` 的记录，`total` 不随筛选变化 |
| 6 | **后台「违规记录查询」页能看到记录** ★ | ✅ | **吕** | **真实浏览器截图** `p2-records.png` |
| 7 | **后台「车辆信息管理」页可用（§6.6 CRUD）** | ⏳ | **汤** | **后端未实现**：`GET/POST /api/vehicles` 均 **404**；前端暂用 localStorage |
| 8 | `GET /api/health`=200 + 冒烟 | ✅ | 吕 | 200 `{"status":"ok"}` |
| 9 | 汤 pytest 四规则 + 吕 TestClient 端点 | ✅ | 汤/吕 | 57 passed（PLAN 目标 ≥70，未达标） |
| 10 | 不卡准确率 / 响应时间 | ✅ | 全员 | 遵守 |

**8 ✅ / 2 ⏳**。两项 ⏳ **均为后端接口，责任人是汤瑾睿**（依据契约 §9 文件归属）。

---

## 五、发现的问题（按严重度）

### 🔴 P0-3：**汤瑾睿的第 1 周任务一项都没开工**（全项目最大风险）

> 本条是本报告初版**完全遗漏**的。用户追问"剩下为什么没完成"时才排查出来。

**实测事实**：`algo/dataset`、`algo/train`、`algo/recognize` **三个目录均不存在**；
汤瑾睿名下只有 `feature/db-schema` 与 `feature/rule-engine` 两个分支，
最新提交是 `8dc6f90`（第 0 天的标注样例与模拟帧）。

对照 `PLAN_4WEEKS.md` §2.2 给他派的 7 项任务：

| # | 汤瑾睿第 1 周任务 | 实测状态 |
|---|---|---|
| 2.1 | **CV 环境搭建（最高风险，要求"先做"）** | ❌ 无 `requirements-algo.txt`，无隔离环境记录 |
| 2.2 | 校园场景数据集（≥120 张标注） | ❌ `algo/dataset/` 不存在 |
| 2.3 | YOLOv8 车辆检测训练跑通 | ❌ `algo/train/` 不存在 |
| 2.4 | 车牌识别 + 绿牌/蓝牌判定 | ❌ `algo/recognize/` 不存在 |
| 2.5 | 识别桩升级为可选真实模型 | ❌ `recognize.py` 仍是纯 stub，无开关 |
| 2.6 | 规则引擎补边界用例 | ❌ 未见新增（测试数仍 57） |
| 2.7 | pytest ≥70 全绿 | ❌ 57，未达 70 |

**为什么这比那 2 项验收更严重**：

1. PLAN §1 明写 **「关键路径 = CV 模型（最慢最险）→ 第 1 周必须启动环境与数据，第 2 周出可替换版本。
   这是全项目唯一真正的风险点」**。任务 2.1 被标注为「**最高风险，先做**」。
2. **它 0% 完成，且第 1 周已结束**。第 2 周的里程碑 M1–M3（图像采集 / 车辆检测 / 车牌识别）
   全部依赖它 —— 风险直接顺延到第 2 周，而第 2 周没有缓冲。
3. 相比之下，那 2 项未完成的验收**有明确任务书、后端工作量小、不阻塞关键路径**。

**建议动作**（需 H 决策，属管理动作而非技术任务）：
- **立即向汤瑾睿确认进度与阻塞点**（是环境装不上？数据采不到？还是时间没排上？）
- 若 CV 环境确有困难，PLAN §2.5 已给降级预案：
  「YOLOv8n 检测 + 车牌模板匹配/`hyperlpr` 备选；实在不行第 2 周继续 stub，**不阻塞其他模块**」
- 把 2.1–2.4 明确列为**第 2 周第一天必须交付**，并把那 2 个后端接口一并排进第 2 周

### 🔴 P0-1：`stores/vehicle.ts` 与 `RecordsView.vue` 注释**过时且误导**
**（本轮已修）**

契约 v1.3 已补齐缺口，但代码里仍写「契约未定义」：

- `frontend/src/stores/vehicle.ts:4` —— 「契约未定义 `vehicle` 表的增删改查接口（**§6 只有 5 个端点**）」
- `frontend/src/views/records/RecordsView.vue:158` —— 页面 info 条仍向用户展示「**契约 §6.4 未定义筛选参数**」

**危害**：契约 v1.4 §6.4/§6.6 白纸黑字定义了这些接口。
这个提示条是**给老师演示时能看到的**，会直接暴露"文档与实现不符"。
**修法**：两处注释/文案改为「**契约已定义，后端待实现**」，语义反转为"等后端"而非"没定义"。
**已修**（见 `git log 7cb967f`）。

### 🔴 P0-4：**契约升版后未同步引用它的文档**（本轮新发现，P0-1 的同类）

P0-1 修的是「代码注释」。但**同一份错误还存在于文档层**：

- `PLAN_4WEEKS.md` §2.4 的验收表仍是 **8 项**（写于契约 v1.2），契约 §11 已 **10 项**
- `PLAN_4WEEKS.md` §2.1/§2.2 的任务清单里，**没有**「筛选参数」「车辆 CRUD」的对应任务
- 连带导致本报告初版引用了错误的分母

**纪律补充**：契约升版时的检查范围要从「代码注释」扩大到
**「所有引用契约的文件」**（`.md` / `.ts` / `.vue` 全量 grep），
且**加验收项必须同步在 PLAN 里派活**——否则会出现「有验收、无任务」的空档。

### 🔴 P0-2：1.9「e2e 扩到前端」未做，1.10 因此不达标
**（本轮已修）**

`scripts/e2e_check.py` 只打后端 API。PLAN 明写「真机起后端 + 前端，走通识别→判定→提醒→第 2 页看到记录」。
**当前缺自动化**——第 2 页的闭环是我**手工**用浏览器验的，没进脚本，下一个人无法一键复现。

### 🟠 P1：`tokens.css` 同步机制**根本不存在**

- `frontend/src/styles/tokens.css` 文件头自称"与 `docs/design/tokens.css` 逐字一致，由 `frontend/scripts/copy-tokens.mjs` 比对"
- **该文件不存在**，`package.json` 也**无对应脚本**
- 实测**当前零漂移**，但这是运气——上游改了忘同步，**无人拦**

**修法**：写 `copy-tokens.mjs` 做比对（或把这两份文件的 diff 并入 `check_tokens.py`），
并在 `package.json` 加 `"check:tokens"` 脚本。

### 🟡 P2：提交信息与实际内容不符

`c574aac`（信息为"契约 v1.3"）里夹带了 `WEEK1_LUHAO_PLAN.md` 的**新增**——
文档首次提交被塞进契约提交，事后追溯困难。违反 `AGENTS.md` 的 `模块: 简述` 规范。

### 🟡 P2：构建产物 1.1MB，有优化空间

`index-P1XpHpZ3.js` **1,100.30 kB**（gzip 353.51 kB），触发 Vite 警告。
不阻塞第 1 周，但第 4 周"打包优化"任务要处理。

---

## 六、超预期完成项

1. **契约缺口主动补齐**（v1.3）—— 发现 §6 无 vehicle CRUD、§6.4 无筛选参数、§1.1 与 PLAN 表述冲突，
   **先改契约再改代码**，完全符合「契约是唯一事实源」的纪律
2. **列名口径收归单一事实源**（v1.4）—— 发现同一字段列名散落 4 处且**已实际分叉**，
   新增 §3.5 映射表并让 `check_tokens.py` **从契约解析白名单**，删掉硬编码
3. **反例自测扩到 13 例**（原 9 例）—— 新增 4 条契约侧反例，证明"改契约就能拦下"非空跑
4. **回签不采信自评** —— 对吴和庆的交付独立复跑，并**有理有据地不采纳**其「设计侧不再有阻塞项」的结论
5. **ruff 修复带行为等价证明** —— 重构 `build_doc_html.py` 后，用「渲染产物与已提交版**逐字比对**」证明行为不变

---

## 七、下一步（按优先级）

| 优先级 | 事项 | 责任 | 说明 |
|---|---|---|---|
| 🔴 **P0-A** | **确认汤瑾睿的 CV 进度与阻塞点** | **H 决策 → 汤瑾睿** | 第 1 周 7 项 0% 完成，且是关键路径。环境装不上？数据采不到？时间没排上？必须先问清 |
| 🔴 **P0-B** | **把 2.1–2.4 重排为第 2 周第一天必达** | H + 汤瑾睿 | CV 是唯一真风险；PLAN §2.5 已有降级预案（YOLOv8n + hyperlpr / 继续 stub） |
| 🔴 **P0-C** | **同步 PLAN 与契约** | 吕浩 | §2.4 验收表 8 → 10 项；§2.1/§2.2 补「筛选参数」「车辆 CRUD」两条任务并派责任人 |
| ⏳ P0-D | 实现 §6.4 筛选 7 参数 + §6.6 车辆 CRUD | 汤瑾睿 | 任务书 `docs/TASK_TANG_v1.3.md` 已发；完成后前端切两处（`README.md` §6） |
| 🟡 P2 | 提交规范 | 全员 | 一提交一主题 |

> **已由本轮完成**（无需再排期）：
> ~~反转两处过时注释/文案~~ → `7cb967f`；
> ~~补 `scripts/e2e_check.py` 前端段~~ → `7cb967f`（12/12 PASS）；
> ~~补 `copy-tokens.mjs`~~ → `88d4893`（3 类漂移反例全拦下）。
>
> **吕浩本人第 1 周任务已 10/10 达标**，剩余未完成项**全部不在他手上**。

### 判断"能不能进第 2 周"的口径

| 视角 | 结论 |
|---|---|
| 吕浩前端 / 集成线 | ✅ **可以进**，10/10 达标，第 2 页可演示 |
| 项目整体 | ⚠️ **可以进，但必须先解决 P0-A** —— CV 是第 2 周关键路径，而现在 0% 起步 |
| 契约 §11 整体验收 | 8/10，剩余 2 项是后端小工作量，**不阻塞主线** |

---

## 八、审查方法说明（可复现）

本审查**不依赖任何自评文档**，全部结论来自实跑：

```bash
# 三项硬门禁
.venv/Scripts/python.exe -m pytest backend/tests -q
.venv/Scripts/python.exe -m ruff check backend scripts
.venv/Scripts/python.exe docs/design/tools/check_tokens.py
.venv/Scripts/python.exe docs/design/tools/selftest_check_tokens.py

# 前端三验证（沙箱需先清代理变量）
cd frontend && node node_modules/vue-tsc/bin/vue-tsc.js --noEmit
node node_modules/vite/bin/vite.js build
node node_modules/eslint/bin/eslint.js .

# 端到端（需先起后端）
cd backend && ../.venv/Scripts/python.exe -m uvicorn app.main:app --port 8000 &
.venv/Scripts/python.exe scripts/e2e_check.py

# 反向验证：筛选参数是否真生效（本次发现后端未实现）
curl --noproxy '*' "http://127.0.0.1:8000/api/records?rule_hit=3"
# → 返回含 rule_hit=1 的记录 ⇒ 筛选被忽略

# 分叉分支内容核验（不是整分支 merge，是按目录摘取）
git diff --stat origin/feature/ui-week1:docs/design origin/main:docs/design
# → 4 文件差异，经查全部是 main 侧主动改造（回签 + 列名收归），非遗漏
```

**关于两个「未合入」分支的澄清**：
`git merge-base --is-ancestor origin/feature/ui-week1 origin/main` 返回非 0，
但这**不代表内容丢失**——经逐文件比对，`docs/design/` 全部 38 个文件在 `main` 上**全部存在**，
且 main 侧是**超集**（多出回签 §8 与列名改造）。属正常的「按目录摘取」合入。
