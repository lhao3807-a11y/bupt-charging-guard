# “桩”点北邮 · 充电桩车位防占系统 — 四周开发任务大纲

> **版本** **v1.1**（2026-09-19 · 第 1 周审查后补齐「有验收、无任务」断层）
> **编写** 吕浩（统筹 / 集成）
> **目标** **一个月（4 周）内完成可演示系统原型**，对齐开发大纲 S2 / S3 阶段（M1–M7）。
> **唯一事实源** `docs/CONTRACT.md` **v1.4**。本文件是**排期与分工**，与契约冲突时以契约为准。
> **配套规范** `AGENTS.md`（每次改动必 commit、交付前测试全过）、`第0天开发文档.md`。

---

## 0. 第 0 天完成情况审计

**结论：第 0 天任务（含共同任务 + 三人分工）已 100% 完成，全部通过验证，可以进入第 1 周。**

### 0.1 散会 Checkpoint（第 0 天文档 §8，5 项全勾）

| # | Checkpoint | 状态 | 证据 |
|---|---|---|---|
| 1 | `CONTRACT.md` 已合入 `main`，三人确认 | ✅ | 契约 **v1.2**（v1.0 为三人签字定稿版），12 节完整（含 §1.1 六页清单、§6 端点、§11 验收清单） |
| 2 | `schema.sql` 已合入，表能建 | ✅ | `backend/sql/schema.sql`（116 行，MySQL 8 语法）+ ORM + 种子；`test_models.py` 通过 |
| 3 | FastAPI 骨架起得来，`/api/health`=200，冒烟测试过 | ✅ | `main.py` 挂载 4 router + lifespan 建表/种子；`test_health.py` 通过 |
| 4 | 三人各讲“我下周提供/调用什么”，互相确认 | ✅ | 接口层已双向对齐：吴的枚举/字段与汤的 `models.py` **逐字一致**，见 0.3 |
| 5 | 第 1 周末 Demo 验收清单已写入 `CONTRACT.md` | ✅ | `CONTRACT.md` **§11**（8 项验收点） |

### 0.2 第 0 天文档 §4「共同任务」5 项

1. 通读文档对齐范围与 MVP —— ✅（MVP 定义已固化进契约 §1）
2. 逐条确认 `CONTRACT.md` —— ✅（四表 / `RecognitionResult` / `ViolationRecord` / 5 端点 / `system_config` 全确认，v0.1→v1.0 补齐 4 处）
3. 约定开发基线 —— ✅（契约 §2：Python 3.11.x、Node 22 LTS、MySQL 8 目标 / SQLite 开发期）
4. 约定仓库结构、分支策略、文件归属、lint/commit 规范 —— ✅（契约 §8/§9 + `AGENTS.md` + `pyproject.toml` 配 ruff/black）
5. 写下第 1 周末 Demo 验收清单 —— ✅（契约 §11）

### 0.3 第 0 天文档 §5「三人分工」完成度

| 成员 | 任务数 | 完成 | 说明 |
|---|---|---|---|
| **吕浩** 5.1 | 7 | ✅ 7/7 | 目录骨架+`.gitignore`、契约 v1.0/v1.1/v1.2、FastAPI 骨架（**并超额实现全部接口层**）、`pyproject.toml`、分支说明、冒烟测试、commit+push |
| **汤瑾睿** 5.2 | 6 | ✅ 6/6 | `schema.sql`+种子、SQLite 等价验证（方案 b）、**规则引擎已从骨架实现为完整版**、4 帧+标注样例、单测（`test_rule_engine.py` 等） |
| **吴和庆** 5.3 | 6 | ✅ 6/6 | `design-tokens.md`（并超额给 `tokens.css` 实现）、组件清单、第 1 页线框、切图规范、`docs/design/**` 已合入 |

### 0.4 三人交叉对齐验证（这是“能不能开工”的关键）

| 对齐项 | 吴（设计） | 汤（后端） | 结论 |
|---|---|---|---|
| 违规规则枚举 | `rule_hit` 1/2/3/0 | `RULE_FUEL_OCCUPY=1` / `RULE_ABNORMAL=2` / `RULE_FULL_NOT_MOVED=3` / `RULE_NORMAL=0` | ✅ 逐字一致 |
| 车型枚举 | 新能源 / 燃油 | `VTYPE_NEW_ENERGY="新能源"` / `VTYPE_FUEL="燃油"` | ✅ 一致 |
| 桩状态枚举 | 空闲 / 充电中 / 已充满 | `PILE_IDLE` / `PILE_CHARGING` / `PILE_FULL` | ✅ 一致 |
| 提醒状态枚举 | 未提醒 / 已提醒 / 失败 | `NOTIFY_PENDING` / `NOTIFY_SENT` / `NOTIFY_FAILED` | ✅ 一致 |
| 阈值参数键 | `full_timeout_min` / `abnormal_park_min` | 同名，经 `get_config_int` 读取 | ✅ 一致 |

### 0.5 当前质量基线（已验证）

| 验证项 | 命令 | 结果 |
|---|---|---|
| 后端测试 | `pytest backend/tests -q` | **57 passed** |
| Python lint | `ruff check backend scripts` | **All checks passed** |
| 设计令牌校验 | `python docs/design/tools/check_tokens.py` | **exit 0，5/5 通过** |

> ⚠️ **唯一遗留风险（不阻塞）**：契约 §2「方案 b」——MySQL 8 真机未验证（本机未装），
> 已明确延至第 2 周接入真实库时补验。SQLite 不校验 `ENGINE=InnoDB`、`ENUM` 等 MySQL 特有语法。

### 0.6 第 1 周开工前已确认的 3 项设计决策（吕浩，2026-09-13）

| # | 决策 | 结论 | 落地位置 |
|---|---|---|---|
| 1 | 是否保留橙档 `--state-caution-*` | **保留**（三违规类型对三色，第 2/4 页需一眼分辨） | `design-tokens.md` §3.4 已标注“已确认保留” |
| 2 | 车型标签是否取车牌底色 | **采纳**（新能源绿 `#0E7A2E` / 燃油蓝 `#1D4FD8`） | `design-tokens.md` §3.3④ 已标注“已确认采纳” |
| 3 | 第 4 页图表选型 | **选用 ECharts**（配色从 `tokens.css` 变量读取，不写死色值） | `component-inventory.md` §3 末尾已标注“已确认选用” |

---

## 1. 四周总览

**起点** 第 0 天已完成（后端端到端骨架可跑通，设计系统就绪，前端未开工）。
**终点** 第 4 周末：**完整可演示系统原型**——CV 模型替换识别桩、真实/沙箱短信、6 页后台全上线、性能达标、结题材料齐备。

| 周 | 主题 | 核心产出 | 里程碑 |
|---|---|---|---|
| **第 1 周** | **端到端闭环 + 前端骨架** | 最小闭环完全跑通；6 页后台骨架 + 第 1/2 页可用；CV 环境与数据集就绪 | 契约 §11 验收清单 **8/8** |
| **第 2 周** | **CV 真模型 + 数据落真库** | YOLOv8 车辆检测 + 车牌识别接入（替换 stub）；MySQL 8 真机切换；6 页全部上线 | 开发大纲 **M1–M3** 完成 |
| **第 3 周** | **真实短信 + 充电模拟器 + 联调** | 短信沙箱升级（预留真实 API 开关）；充电状态模拟器/OCPP 适配层；全链路联调 | **M4–M6** 完成 |
| **第 4 周** | **性能与稳定 + 结题材料** | 准确率调优、响应 ≤5s、稳定 ≥24h；技术报告/用户手册/结题报告；答辩材料与演示脚本 | **M7 完成 + S4 验收** |

**关键路径**：CV 模型（最慢最险）→ 第 1 周必须启动环境与数据，第 2 周出可替换版本。**这是全项目唯一真正的风险点**，其余模块已用桩替身解耦，不阻塞。

---

## 2. 第 1 周详细任务

> **周主题**：**端到端闭环 + 前端骨架**
> **周末目标**：契约 §11 的 8 项验收全部打勾；后台第 1、2 页可操作；CV 环境与数据集就绪。
> **本周不卡**：准确率、响应时间（CV 仍为桩替身，短信仍为沙箱）——**闭环优先**。

### 2.1 吕浩（统筹 / 集成 / 前端）

| # | 任务 | 交付物 | 验收标准 |
|---|---|---|---|
| 1.1 | **前端脚手架**：Vue3 + Vite + TS + Element Plus + Vue Router + Pinia | `frontend/` 可 `npm run dev` 启动 | 访问首页出现后台骨架（侧边栏 220px + 顶栏 56px + 内容区） |
| 1.2 | 接入设计令牌与主题 | `frontend/src/styles/tokens.css`（从 `docs/design/tokens.css` 引入）、`main.ts` 引入顺序正确 | 页面主色 = `#1668E3`；**无裸色值**（沿用吴的令牌体系） |
| 1.3 | **AppShell + 通用件**：`AppShell` / `PageHeader` / `FilterCard` / `DataCard` / `StatusTag` / `StatusPill` / `FormDialog` / `ConfirmDialog` / `EmptyState` | `frontend/src/components/` | 按 `component-inventory.md` §2 规格表实现（高度/圆角/字号取令牌） |
| 1.4 | 侧边导航 6 项（第 6 页置灰） | 路由表 + `el-menu` | 顺序 = 契约 §1.1，第 6 项 `disabled` + “第 1 周预留” |
| 1.5 | **API 客户端 + 代理**：封装 `recognize` / `judge` / `notify` / `records` / `health` | `frontend/src/api/` | Vite proxy 指向 `127.0.0.1:8000`；**TypeScript 类型手写对齐契约 §4/§5** |
| 1.6 | **第 2 页「违规记录查询」（Demo 验收项，最高优先级）** | `views/records/` | 筛选（车牌/车型/桩ID/`rule_hit`/`notify_status`/时间范围）+ 表格 + 分页（用响应 `total`）+ 「发送提醒」调 `POST /api/notify` |
| 1.7 | **第 1 页「车辆信息管理」** | `views/vehicle/` | 表格列 = `vehicle` 全字段；车牌/手机号/时间用等宽字族；新增/编辑弹窗（车牌主键编辑态只读）；删除二次确认 |
| 1.8 | 第 3 / 4 / 5 页占位路由 | 路由 + 空态 | 能点进、有标题、显示“开发中”，不留白屏 |
| 1.9 | 联调与 e2e 回归 | `scripts/e2e_check.py` 扩到前端 | 真机起后端 + 前端，走通「识别→判定→提醒→第 2 页看到记录」 |
| 1.10 | 本周集成与合入 | `main` 干净、CI 全绿 | 三人分支全部合入，57+ 测试通过，`check_tokens.py` exit 0 |

### 2.2 汤瑾睿（后端 / DB / 算法）

| # | 任务 | 交付物 | 验收标准 |
|---|---|---|---|
| 2.1 | **CV 环境搭建**（最高风险，先做） | 隔离环境 + `requirements-algo.txt` | **Python 3.11**（勿用 3.13）装通 `ultralytics` + `paddleocr`/`hyperlpr`；记录可复现安装步骤 |
| 2.2 | **校园场景数据集**：采集/收集充电桩车位图像 + 标注 | `algo/dataset/` + 标注文件 | 覆盖：燃油占位 / 新能源充电中 / 新能源空闲久停 / 已充满；每类 ≥ **30 张**（合计 ≥120） |
| 2.3 | YOLOv8 车辆检测**训练跑通**（可先用预训练权重推理） | `algo/train/detect.py` + 权重 | 能在测试图上框出车牌/车辆区域，输出 `bbox` |
| 2.4 | 车牌识别 + **绿牌/蓝牌判定** | `algo/recognize/plate.py` | 输入帧 → 输出 `plate` + `vtype`（绿牌=新能源 / 蓝牌=燃油），**结构与 `RecognitionResult` 完全一致** |
| 2.5 | **识别桩升级为“可选真实模型”** | `backend/app/routers/recognize.py` 改造 + 开关 | 经 `system_config` 或环境变量切换 `stub` / `real`；**默认仍 stub**，保证 demo 不被模型拖累 |
| 2.6 | 规则引擎补充边界用例 | `tests/test_rule_engine.py` 增补 | 覆盖：桩不存在 / 车牌未绑定 / 阈值为 0 / 时间倒挂 |
| 2.7 | 后端测试与 lint 全绿 | — | `pytest` 全过（目标 **≥ 70**）、`ruff`+`black` 全过 |
| **2.8** | 🆕 **`GET /api/records` 筛选参数**（契约 §6.4） | `backend/app/routers/records.py` | 7 参数（`plate`/`vtype`/`pile_id`/`rule_hit`/`notify_status`/`start_time`/`end_time`）**AND 关系**；`plate`/`pile_id` 模糊匹配；`end_time` 只给日期时按当日 `23:59:59.999999` 解释；**`total` 必须反映筛选后总数** |
| **2.9** | 🆕 **车辆 CRUD 四端点**（契约 §6.6） | `backend/app/routers/vehicles.py` | `GET/POST /api/vehicles`、`PUT/DELETE /api/vehicles/{plate}`；**`plate` 主键不可改**（`VehicleUpdate` 不含该字段）；重复 `POST` → **`409`**；`DELETE` → **`204` 且不级联删违规记录**（`occupation_record.plate` **不加 FK**） |

> **任务书**：2.8 / 2.9 的完整口径、curl 验收命令与专项测试要求见 `docs/TASK_TANG_v1.3.md`。
> 这两项是契约 v1.3 新增的验收项（§11 第 5、7 条），**v1.0 计划里遗漏了任务分派**，v1.1 补入。

### 2.3 吴和庆（UI / 美术）

| # | 任务 | 交付物 | 验收标准 |
|---|---|---|---|
| 3.1 | **第 2–5 页线框**（4 页，照第 1 页风格样板对齐） | `docs/design/wireframes/` 4 个 HTML | 每页含：页头 + 筛选/操作区 + 主体（表格/卡片/图表）+ 空态；**表格列严格照契约字段** |
| 3.2 | 第 4 页图表配色交付 | `component-inventory.md` 或线框内标注 | 明确 ECharts 序列顺序（红→橙→黄→绿）、图例/坐标轴/提示框样式 |
| 3.3 | 图标与 Logo（可选，第 1 周不阻塞） | `docs/design/assets/icons/`、`logos/` | 遵循 `README.md` §2.2：`24×24`、`stroke-width:1.5`、**必须用 `currentColor`**；命名 kebab-case |
| 3.4 | 空状态/插画（可选） | `docs/design/assets/illustrations/` | SVG 优先，命名合规 |
| 3.5 | 校验脚本全绿 | `check_tokens.py` | **exit 0**；新增线框的快照与 `tokens.css` 逐条一致、无裸色值 |
| 3.6 | 设计走查（周末） | 走查记录 | 对照 `component-inventory.md` §5 跨页一致性检查单 9 项，逐项打勾 |

### 2.4 第 1 周验收标准（对齐契约 §11，**10 项**）

> ⚠️ **v1.1 修订（2026-09-19）**：本节原为 **8 项**，写于契约 v1.2 时期。
> 契约 v1.3 新增两条验收（筛选参数、车辆 CRUD）后 §11 已为 **10 项**，本节此前**未同步**，
> 导致「有验收、无任务」的空档（第 5/7 条在 §2.1/§2.2 任务清单里找不到对应任务）。
> 现已补齐 10 项并**逐条指派责任人**。

| # | 契约 §11 验收点 | 责任 | 第 1 周目标 | 状态 |
|---|---|---|---|---|
| 1 | `POST /api/recognize` 返回合规 `RecognitionResult` | 汤/吕 | ✅ 已过（stub），本周验证 real 开关 | ✅ |
| 2 | `POST /api/judge` 四规则正确（三命中 + 正常 null） | 汤 | ✅ 已过，本周补边界用例 | ✅ |
| 3 | `POST /api/notify` 沙箱写库 + 日志落盘 | 吕 | ✅ 已过 | ✅ |
| 4 | `GET /api/records` 能查到该记录，分页 `total` 正确 | 吕 | ✅ 已过 | ✅ |
| 5 | **`GET /api/records` 筛选参数（§6.4）生效** | **汤** | 🆕 v1.3 新增，见 §2.2 任务 2.8 | ⏳ |
| 6 | **后台「违规记录查询」页面能看到该条记录** | **吕** | 🔴 **本周必达（Demo 验收项）** | ✅ |
| 7 | **后台「车辆信息管理」页面可用（§6.6 CRUD）** | **汤** | 🆕 v1.3 新增，见 §2.2 任务 2.9 | ⏳ |
| 8 | `GET /api/health`=200，冒烟测试通过 | 吕 | ✅ 已过 | ✅ |
| 9 | 汤 pytest 四规则全过；吕 TestClient 端点+沙箱全过 | 汤/吕 | 57 passed，本周扩到 ≥70 | ⏳ |
| 10 | 本周不卡准确率 / 响应时间 | 全员 | ✅ 遵守 | ✅ |

> **第 1 周的核心是第 6 项**：后端闭环已经通了，前端一接上，全项目就从“后端能跑”变成“**能给老师看**”。

> **纪律（v1.1 补）**：契约 §11 **新增验收项时，必须同时在本节与 §2.1/§2.2 的任务清单里派活**，
> 否则会出现「有验收、没人做」。改契约后须全量 grep 所有引用契约的文件（`.md`/`.ts`/`.vue`）。

### 2.5 第 1 周风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| CV 环境装不上（`paddleocr`/`ultralytics` 依赖冲突） | 第 2 周无模型可换 | 第 1 周**第一天就试装**；失败立即降级：YOLOv8n 检测 + 车牌模板匹配/`hyperlpr` 备选；实在不行第 2 周继续 stub，**不阻塞其他模块** |
| 数据集不足（校园场景难采集） | 模型效果差 | 用 `algo/tools/make_mock_frames.py` 扩增合成帧 + 校园实拍结合；标注用 LabelImg/CVAT |
| 前端工期被设计走查拖慢 | 第 5 项达不成 | 第 1/2 页**优先于**图标/插画；图标第 1 周允许全用 `@element-plus/icons-vue`（吴已在 `README.md` §4 明确允许） |
| 三人分支再度分叉 | 合并事故 | **每次开工先 `git pull --rebase`**；合并前跑 `git merge-base --is-ancestor origin/main <分支>`，非后代则按目录摘取 |

---

## 3. 第 2–4 周任务大纲（细化至第 1 周后会更新）

### 第 2 周 · CV 真模型 + 数据落真库

| 线 | 任务 | 里程碑 |
|---|---|---|
| 汤 | YOLOv8 微调（校园数据集）；HyperLPR/PaddleOCR 车牌识别调优；**MySQL 8 真机验证**（跑 `schema.sql`，补验 `InnoDB`/`ENUM`）；充电状态**模拟器**初版 | **M1 图像采集、M2 车辆检测、M3 车牌识别** |
| 吕 | 第 3 页「充电状态展示」（卡片+表格）；第 4 页「报警统计」（**ECharts** 柱/折线/饼）；第 5 页「系统参数配置」（阈值行内编辑）；识别桩 `real` 模式切换联调 | 6 页前端全部上线 |
| 吴 | 第 6 页导航占位与素材；图标替换第 1 周临时图标；整体视觉走查与细节打磨 | 设计走查通过 |

### 第 3 周 · 真实短信 + 模拟器 + 全链路联调

| 线 | 任务 | 里程碑 |
|---|---|---|
| 汤 | 短信**沙箱 → 真实 API**（接第三方，保留 `system_config` 开关，答辩可回落沙箱）；充电状态模拟器完整版（状态机 + OCPP 适配层预留）；异常重试与失败落库 | **M4 充电状态模拟、M6 短信提醒** |
| 吕 | 三端联调（后端+前端+算法）；第 6 页「实时识别预览」接预留端点 `GET /api/frame/latest`；全链路 e2e 自动化；错误处理与提示完善 | 闭环稳定可演示 |
| 吴 | 演示素材（答辩配图、`demo-` 前缀）；界面文案与信息层级优化 | 素材齐备 |
| 全员 | **M5 规则引擎终版评审**（阈值可配、边界完备） | **M5 完成** |

### 第 4 周 · 性能稳定 + 结题材料

| 线 | 任务 | 里程碑 |
|---|---|---|
| 汤 | 准确率调优（扩充测试集、模型微调、目标跟踪去重）；**响应 ≤ 5s**；**稳定运行 ≥ 24h** 压测 | S3 性能指标达标 |
| 吕 | 后端 **MySQL 8 生产配置**（连接池、索引）；前端打包优化与部署脚本；技术报告 / 用户手册 | **M7 完成** |
| 吴 | 演示 PPT 视觉、系统截图美化、答辩画册 | 答辩材料 |
| 全员 | 系统验收；实验数据整理；**技术报告 / 用户手册 / 结题报告**；软著材料；竞赛答辩演练 | **S4 验收与结题** |

---

## 4. 里程碑对照（本计划 ↔ 开发大纲）

| 大纲里程碑 | 内容 | 本计划落点 |
|---|---|---|
| M1 | 视频 / 图像采集模块 | 第 2 周（汤 2.x） |
| M2 | 车辆检测模块 | 第 2 周（汤） |
| M3 | 车牌识别模块（含车型判定） | 第 2 周（汤） |
| M4 | 充电状态模拟模块 | 第 3 周（汤） |
| M5 | 违规判断规则引擎 | **第 0 天已实现**，第 3 周终版评审 |
| M6 | 短信提醒模块 | **第 1 周沙箱**（已完成）→ 第 3 周真实 API |
| M7 | 后台管理平台（Web） | 第 1 周 2 页 → 第 2 周 6 页 → 第 4 周优化 |

> 说明：**M5 规则引擎与 M6 沙箱版在第 0–1 周即完成**，是本计划相比原大纲提前的部分，
> 目的是尽早锁定业务闭环正确性，把风险集中在 CV 模型上。

---

## 5. 每日 / 每周纪律

**每日**
- 开工先 `git pull --rebase`，收工必 commit（信息格式 `模块: 简述`）。
- 交付前跑：`pytest backend/tests -q` + `ruff check backend scripts` + `python docs/design/tools/check_tokens.py`，**全绿才 push**。

**每周五**
- 三人各 5 分钟同步：**已完成 / 卡点 / 下周提供什么**。
- 吕浩统一合入，确认 `main` 上测试全过。
- 契约如有改动，**先改 `CONTRACT.md` → @全员 + 升版本号**，再动代码。

**红线（不可越）**
- 阈值一律从 `system_config` 读，**严禁硬编码**（契约 §7）。
- 页面里**不出现裸色值**，一律 `var(--token)`。
- 表格列以契约字段为准，**不自造字段、不自造枚举**（页面增删须先改契约 §1.1）。
- 沙箱内跑本地 HTTP 服务前先 `os.environ["NO_PROXY"]="*"`（第 0 天踩过的坑）。

---

## 6. 变更记录

| 版本 | 日期 | 变更 | 发起人 |
|---|---|---|---|
| v1.1 | 2026-09-19 | 第 1 周审查时发现**「有验收、无任务」的计划断层**：契约 v1.3 给 §11 新增了两条验收（记录筛选参数、车辆 CRUD），但本文件的 §2.4 验收表仍是 8 项、§2.1/§2.2 任务清单里也**没有对应任务** → 汤瑾睿按 PLAN 干活，天然不会做这两项，直接导致 §11 停在 8/10。修订：① §2.4 验收表 8 → **10 项**，并**逐条指派责任人**、标注状态；② §2.2 新增任务 **2.8**（筛选参数）与 **2.9**（车辆 CRUD）；③ 补纪律条目「契约新增验收项时必须同时在任务清单派活，改契约后须全量 grep 引用契约的文件」 | 吕浩 |
| v1.0 | 2026-09-13 | 首版。① 审计第 0 天任务完成情况（Checkpoint 5/5、共同任务 5/5、三人分工 18/18、交叉对齐 5/5、质量基线 57 passed）；② 确认 3 项设计决策（保留橙档 / 车型取车牌底色 / 选用 ECharts）；③ 将一个月拆为四周，给出总览表、第 1 周详细任务（含 3 人逐条任务 + 验收标准 + 风险对策）与第 2–4 周大纲 | 吕浩 |
