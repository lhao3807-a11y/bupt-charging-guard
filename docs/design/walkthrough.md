# 设计走查记录 · 第 1 周

> **走查人** 吴和庆（UI / 美术）
> **走查时间** 2026-09-19（第 1 周周末）
> **走查对象** `docs/design/` 全量：5 个线框 + 21 个素材 + 3 份文档 + 1 个校验脚本
> **走查依据** `component-inventory.md` §5「跨页一致性检查（交付前自查）」9 项、`CONTRACT.md` v1.2、`design-tokens.md` v1.0
> **对应任务** `PLAN_4WEEKS.md` §2.3 的 3.5（校验脚本全绿）、3.6（设计走查）
> **结论** **9/9 项通过**（其中 6 项已进自动化）；走查 + **交付前终检**共发现 **8 项问题，全部当日修复**（F8 为终检补获）；遗留 **3 项待确认**（见 §5），均不阻塞第 1 周。

---

## 1. 走查方式

不靠「看一遍觉得没问题」，三路并用：

| 方式 | 做法 | 覆盖 |
|---|---|---|
| **自动断言** | 跑 `python docs/design/tools/check_tokens.py`（本周从 5 项扩到 7 项），exit 0 才算过 | 检查单第 1/2/3/6/7/8/9 项 |
| **人工比对** | 拿 `CONTRACT.md` 字段表、`design-tokens.md` §3.3 映射表逐列逐色对照 | 检查单第 4/5 项 |
| **反例自测** | 故意把线框改坏 8 处，确认脚本每次都拦下（见 §3） | 验证断言不是空跑 |

```bash
# 仓库根目录
python docs/design/tools/check_tokens.py     # 期望 exit 0
```

---

## 2. 检查单逐项结论（component-inventory.md §5，9 项）

| # | 检查项 | 结论 | 证据 |
|---|---|---|---|
| 1 | 6 页的侧边导航顺序一致，第 6 页置灰 | ✅ | 5 个线框导航文案与 `CONTRACT.md` §1.1 顺序逐字一致；每个文件恰有 1 个 `is-active` 与 1 个 `is-reserved`；脚本 [5] 项自动比对 |
| 2 | 所有表格：表头 `--fill-light`、行线 `--border-light`、悬浮 `--brand-primary-bg` | ✅ | 5 页的 `thead th` / `tbody td` / `tr.is-hover td` 三条声明均由脚本 [5] 项正则断言；5 页共用同一份表格 CSS，无页级分叉 |
| 3 | 所有车牌/桩 ID/手机号/时间：等宽字族 + `tabular-nums` | ✅ | 第 1 页 18 处、第 2 页 32 处、第 3 页 48 处、第 4 页 12 处、第 5 页 8 处 `.mono`；全部走共享 `.mono` 类（`--font-family-mono` + `--font-variant-numeric`） |
| 4 | 所有状态标记：白底上用 `-text` 档，实心块上橙/黄配深字 | ✅（人工） | 白底可见元素一律 `-text` / `-bg` / `-border` 三档组合（`StatusPill` 类名 `.pill-danger/caution/warning/success/info`）；唯一实心块是第 4 页统计卡 3px 色条与环形图，**其上不承载文字**，故不涉及「橙黄配深字」；对比度由脚本 [2] 项断言（caution text 5.52、warning text 5.24） |
| 5 | 所有违规色：严格按 `design-tokens.md` §3.3 映射，无自选色 | ✅（人工） | 逐页核对：① `rule_hit` 1/2/3 → danger/caution/warning（第 2 页表格、第 4 页统计卡与柱状图完全同序）；② `notify_status` 未提醒→warning、已提醒→success、失败→danger；③ `charging_pile.status` 空闲→info、充电中→primary、已充满→caution；④ `vtype` 走 `--plate-*`。**无一处自选色** |
| 6 | 所有间距：只取 `--space-*` 与 `--layout-*` | ✅ | 脚本 [5] 项扫描全部 `padding/margin/gap`：5 页均为 0 处 ≥4px 的裸 px。仅 `code` 标签内衬 `1px`（字内微调，非栅格间距） |
| 7 | 页面里没有裸色值 | ✅ | 脚本 [3] 项：5 页快照外裸色值 **0 处**，引用令牌未定义 **0 个**，内联快照与 `tokens.css` 漂移 **0 项** |
| 8 | 空态 `el-empty`、加载态 `el-skeleton` 都有 | ✅ 空态 / ➖ 加载态 | 5 页均有空态区块（**第 1 页为本次补齐，见 F1**）；加载态为前端实现细节，线框不画骨架屏，第 2 周随前端实现一并交付（不适用项，非遗漏） |
| 9 | 表格列名与契约字段一一对应，没有自造字段 | ✅（终检修正后） | 脚本 [5] 项按 `CONTRACT_COLUMNS` 白名单逐页比对：列名**无越界、关键列齐全**（vehicle 6 列、occupation_record 9 列、charging_pile 6 列、统计下钻 5 列、system_config 4 列）。**交付前终检发现第 2 页 `plate` 与契约/第 4 页写法分叉（F8），已统一为「车牌」并加同表跨页断言**；列名→契约字段的逐条映射见 `component-inventory.md` §4.0 |

**自动校验结果摘要**（`check_tokens.py`，exit **0**）：

```
[1] 令牌总数 165，重复定义 0，必需令牌 91/91 齐全，取值非法 0
[2] 对比度：5 组状态色 text 档白底 5.24~7.75 全 OK；配对 12 项全 OK
[3] 5 页线框：快照漂移 0 项，引用令牌未定义 0 个，裸色值 0 处
[4] 素材 21 个：命名不合规 0；图标 16 个全部 currentColor / 24×24 / 1.5 描边
[5] 跨页一致性：5 页的导航/高亮/空态/表格规格/列名/间距 全部一致
[警告] 3 项（不阻断，均为已知且刻意保留的设计取舍）
```

3 条警告都是**设计上主动接受**的，不是遗留缺陷：

1. `--state-caution-solid` 白底 2.38 —— 该档本来就不允许用于白底，只做填充块（`design-tokens.md` §3.2）；
2. `--state-warning-solid` 白底 1.90 —— 同上；
3. `--text-disabled` 白底 1.59 —— 依 WCAG 1.4.3 禁用态豁免，且不得承载有效信息。

---

## 3. 反例自测：断言不是空跑

脚本：`docs/design/tools/selftest_check_tokens.py`（把下面 9 处改动依次注入，确认每次都被拦下，然后自动还原）。
任何人都可以复跑，用来判断 `check_tokens.py` 的「全部通过」是**真的合规**还是**断言没查到**。

```bash
python docs/design/tools/selftest_check_tokens.py     # 期望 exit 0
```

| 注入的违规 | 脚本 exit | 是否拦下 |
|---|---|---|
| 表格列名自造（`自造字段`） | 1 | ✅ 表格列名超出契约字段白名单 |
| 导航文案与契约不符 | 1 | ✅ 导航 6 项与契约顺序不一致 |
| 当前页高亮丢失 | 1 | ✅ 应恰有 1 个 is-active |
| 空态区块缺失 | 1 | ✅ 缺少空态区块 |
| 间距写 `padding: 20px 24px 40px` | 1 | ✅ 间距出现裸 px 值 |
| 写裸色值 `color: #A8071A` | 1 | ✅ 出现裸色值 |
| tokens 快照改一位（`#1668E4`） | 1 | ✅ 令牌快照与 tokens.css 不一致 |
| 图标写死 `stroke="#A8071A"` | 1 | ✅ 图标不得写死色值 |
| 第 4 页把「车牌」写成「车牌号」（与第 2 页分叉） | 1 | ✅ 列名分叉（同时触发白名单越界） |

**实跑结果：9/9 全部拦下；还原后复跑 exit 0。** 说明「全绿」是真的全绿，不是脚本没查到。

> 第 9 条用例是**交付前终检**补入的（见 F8）：它的价值恰恰在于**前 8 条都拦不住这一类错**
> —— 白名单和线框出自同一手，写错时两边一起错。只有跨页交叉比对才能发现。

---

## 4. 发现项与处理（8 项：走查期 7 项 + 交付前终检 1 项，均当日修复）

| # | 发现 | 严重度 | 处理 |
|---|---|---|---|
| **F1** | **第 1 页线框缺空态区块** —— 第 0 天交付样板页时遗漏，与其余 4 页不一致 | 中 | 已补空态（含插画、双触发原因文案、引导按钮），并新增标注第 9 条说明；同时把「空态必查」写进脚本 [5] 项，**以后漏了直接失败** |
| **F2** | 第 5 页参数键/阈值数字**页面内重复声明等宽字族**，未复用共享 `.mono` 类 | 低 | 删除页级 `font-family: var(--font-family-mono)`，改用 `class="key mono"`；由脚本 [5] 项「`.mono` 使用数 ≥ 4」抓出 |
| **F3** | 契约 **v1.2** 两处口径变更（`/api/notify` 入参需 `id`、`/api/judge` 命中即落库 + 去重）会影响第 2 页展示 | 高 | 第 2 页线框同步：**`id` 列必须展示**（作为操作与去重的定位键）、筛选下拉不给 `rule_hit = 0`、标注写明**前端不得自行去重** |
| **F4** | 图表序列色若与第 2 页表格色不同序，用户要重新学一遍颜色 | 中 | 写入 `component-inventory.md` §3.1 ①，明确「序列顺序即严重度顺序，跨页不换色」 |
| **F5** | 环形图「按桩占比」原方案用违规色，会暗示「份额大的桩更严重」（份额 ≠ 严重度） | 中 | 分类份额改用主色梯度（`--brand-primary` → `-light-3` → `-light-5` → `-light-7` → `--state-info-solid` → `--state-info-border`），写入 §3.1 ② |
| **F6** | 第 4 页统计卡示意值需自检「分项之和 = 总数」 | 低 | 线框数据 23 + 19 + 26 = 68 = 总数 ✅；并把该自检口径写进 §3.1 ⑥，提醒答辩前复核 |
| **F7** | **SVG 素材注释里写 `var(--token)` 导致 XML 解析失败**（XML 注释内不允许连续两个连字符） | 高 | 21 个素材全部改为不含 `--` 的注释；用 `xml.etree` 逐个复验 **21/21 解析通过**。已在素材生成说明里留痕 |
| **F8** | **第 2 页 `plate` 列名与契约/文档分叉**（**交付前终检发现**）—— 线框表头写「车牌号」，但 `occupation_record` 的契约说明（`CONTRACT.md` §3.3）是「车牌」，`component-inventory.md` §4.2 的列清单写的也是「车牌」，第 4 页下钻表同样写「车牌」。**三方分叉，而自动校验抓不到**：白名单与线框出自同一手，写错时两边一起错 | 中 | 统一为**「车牌」**（第 2 页 4 处：筛选、表头、两条标注）；`component-inventory.md` 升 **v1.2**，新增 **§4.0 列名 ↔ 契约字段映射表**（显示标签 → 契约表.字段 → 契约说明，吕浩写 `el-table-column prop` 直接照抄），并把「手机号 / 状态 / ID」三处登记为**显示标签简写、非新增字段**；`check_tokens.py` 增 **同表跨页列名一致性**断言（`SHARED_TABLE_PAGES`）+ selftest 第 9 条用例，**这类错以后会被直接拦下** |

> F1、F2 是**自动校验抓出来的**，F3~F6 是**拿契约逐条比对抓出来的**，F7 是**素材复验抓出来的**。
> 走查若不自动化、不回头对契约，这 7 条里至少 F1、F3、F5 会漏到前端实现阶段才暴露。

---

## 5. 待确认项（3 项，不阻塞第 1 周）

| # | 事项 | 需谁确认 | 影响 |
|---|---|---|---|
| **T1** | 「改阈值**不回溯重算**已落库的历史记录」——第 5 页保存确认框里写了这句，是设计侧按「判定发生在落库时刻」推出的行为 | **汤瑾睿** | 若引擎会回溯重算，第 5 页该句必须删除并另写说明，否则属于**页面文案与实现不符** |
| **T2** | 保存参数需要写接口（`PUT/POST /api/config` 之类），**契约 §6 未定义** | **吕浩** | 第 1 周若不做编辑交互，本页可先只读展示；要做编辑须走「先改 `CONTRACT.md` → @全员 → 升版本号」 |
| **T3** | 第 1 页手机号是否脱敏显示（`138****6621`） | **吕浩** | 仅改展示不改字段，但契约未规定；本次线框沿用脱敏，若不采纳直接显示明文 |

另有 2 项**已由契约 v1.2 自动解决**，此处记录以示闭环：原「`/api/notify` 无法定位记录」→ v1.2 入参加 `id`；原「轮询会导致记录爆炸」→ v1.2 定去重口径。

---

## 6. 跨页数据自洽核对（演示前必查）

三页的示意数据必须能互相解释，否则答辩现场一眼穿帮。本次已逐条对上：

| 第 2 页记录 | 第 3 页桩状态 | 是否自洽 |
|---|---|---|
| 记录 1：京A12345 燃油 · PILE-001 · 燃油占位 | PILE-001 空闲、无绑定车牌 | ✅ 燃油车占用，车不绑定充电桩 |
| 记录 2：京A88888 燃油 · PILE-007 · 燃油占位 | PILE-007 空闲、无绑定车牌 | ✅ 同上 |
| 记录 3：京AD67890 新能源 · PILE-003 · 异常占位 | PILE-003 空闲、绑定京AD67890 | ✅ 「停了没充」正是规则 ② 的判定输入 |
| 记录 4：京AD24680 新能源 · PILE-005 · 充满未移车 | PILE-005 已充满、绑定京AD24680、12:10 充满 | ✅ 命中时间 12:30 在充满之后 |
| 记录 5：京AD12345 新能源 · PILE-002 · 充满未移车 | PILE-002 已充满、绑定京AD12345、08:55 充满 | ✅ 同上 |
| 记录 6：京AD13579 新能源 · PILE-006 · 异常占位 | PILE-006 空闲、绑定京AD13579 | ✅ 同记录 3 |

**另外两条隐含规则也已核对**：

1. **车型与规则必须语义自洽**：`rule_hit = 1`（燃油占位）必须是燃油车；`rule_hit = 2 / 3` 必须是新能源车。第 2、4 页数据已按此构造。
2. **第 3 页统计口径**：空闲 4 + 充电中 2 + 已充满 2 = 8 个桩，与概览卡一致；第 4 页 23 + 19 + 26 = 68 与总数一致。

---

## 7. 结论

- **`python docs/design/tools/check_tokens.py` → exit 0**，满足 `AGENTS.md`「交付前所有测试与验证全部通过」。
- **交付前终检**又跑了一轮「拿契约逐字对列名」，抓出 **F8**（第 2 页 `plate` 与文档/契约/第 4 页三方分叉）并修复。
  这一类错**前 8 条反例用例都拦不住** —— 所以补了第 9 条「同表跨页列名」断言，而不是只改对了这一处。
- 第 1 周设计侧任务 `PLAN_4WEEKS.md` §2.3 的 **3.1~3.6 全部完成**：4 页线框、图表规格、素材、校验全绿、走查记录。
- 5 页线框（第 1~5 页）已就位，**第 2 页「违规记录查询」即契约 §11 第 5 项 Demo 验收项**，设计侧不再有阻塞项。
- 前端实现可直接照 `component-inventory.md` §2 组件规格总表 + §3.1 图表规格 + `tokens.css` 取用；**页面里不得出现裸色值，改坏会被脚本拦下**。
