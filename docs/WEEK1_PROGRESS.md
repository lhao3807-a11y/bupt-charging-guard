# 第 1 周总进度汇总报告

> **报告时间** 2026-09-19 22:22 · **编写** 吕浩（统筹 / 集成）
> **周主题** 端到端闭环 + 前端骨架
> **基线** `docs/CONTRACT.md` v1.4 · `docs/PLAN_4WEEKS.md` v1.1
> **方法** 全部数据为**本轮实测复跑**，不引用他人自评与 commit message

---

## 一、总体结论

**第 1 周目标基本达成，端到端闭环已在真实服务上跑通，契约 §11 验收 10/10。**

| 维度 | 计划目标 | 实测结果 | 判定 |
|---|---|---|---|
| 端到端闭环 | 识别→判定→提醒→后台可见 | `12/12 PASS`（真实双服务） | ✅ **达标** |
| 契约 §11 验收 | 10 项全打勾 | **10/10** | ✅ **达标** |
| 三人任务 | 吕 10 + 汤 9 + 吴 6 = 25 | **24/25**（1 项待前端切换） | ⚠️ **96%** |
| 质量基线 | 全绿才 push | 后端 4 项 + 前端 4 项**全 exit 0** | ✅ **达标** |
| 关键路径（CV） | 环境与数据就绪 | **代码齐全，但本机无产物凭据** | ⚠️ **待复现** |

**一句话**：主线（能演示）成立，**唯一真风险是 CV 产物无独立凭据**。

---

## 二、质量基线实测（本轮复跑，非引用）

### 后端（`.venv/Scripts/python.exe`，Python 3.11）

| # | 命令 | 结果 |
|---|---|---|
| 1 | `pytest backend/tests -q` | ✅ **132 passed**（1 warning，starlette 弃用别名，非阻断） |
| 2 | `ruff check backend scripts` | ✅ **All checks passed!** |
| 3 | `ruff format --check backend scripts` | ✅ **26 files already formatted** |
| 4 | `python docs/design/tools/check_tokens.py` | ✅ **exit 0**（3 项对比度警告为设计侧已知、非阻断） |
| 5 | `python docs/design/tools/selftest_check_tokens.py` | ✅ **13/13 全部拦下**，还原后 exit 0 |

### 前端（Node 22.22.2）

| # | 命令 | 结果 |
|---|---|---|
| 6 | `copy-tokens.mjs --check` | ✅ **零漂移 165/165** |
| 7 | `vue-tsc --noEmit` | ✅ exit 0（零错误） |
| 8 | `eslint .` | ✅ exit 0（0 错 0 警） |
| 9 | `vite build` | ✅ exit 0（5.04s；`index.js` 1,100 kB / gzip 353 kB） |

### 闭环（真实双服务，8000 + 5173）

| 段 | 内容 | 结果 |
|---|---|---|
| §1–8 | 后端：health / recognize / judge / notify / records / 容错 404 ×2 / 规则① | ✅ 8/8 PASS |
| §9–12 | 前端：首页 `#app` / Vite 代理 / 第 2 页数据源含目标记录 / `main.ts` | ✅ 4/4 PASS |
| **合计** | | ✅ **12/12 ALL PASSED** |

> 实测证据：judge 落库 `id=8`（`rule_hit=3`）→ notify 置「已提醒」→ 经 Vite 代理在第 2 页数据源中查到该记录。

### 契约两条新增验收的实调（v1.3 补齐项）

| 项 | 实测 | 判定 |
|---|---|---|
| §6.4 `rule_hit=3` 纯度 | `total=5`，返回集合 `{3}` | ✅ PASS |
| §6.4 `total` 随筛选变化 | 无筛选 `total=6` → `rule_hit=1` 时 `total=1` | ✅ PASS |
| §6.6 `GET /api/vehicles` | `200` | ✅ PASS |
| §6.6 不存在车牌 PUT | `404` | ✅ PASS |

---

## 三、契约 §11 验收逐项状态（10/10）

| # | 验收点 | 责任 | 状态 | 依据 |
|---|---|---|---|---|
| 1 | `recognize` 返回合规 `RecognitionResult` | 汤/吕 | ✅ | e2e §2 PASS |
| 2 | `judge` 四规则正确（三命中 + 正常 null） | 汤 | ✅ | e2e §3/§8 PASS |
| 3 | `notify` 沙箱写库 + 日志落盘 | 吕 | ✅ | e2e §4 PASS |
| 4 | `records` 能查到该记录，分页 `total` 正确 | 吕 | ✅ | e2e §5 PASS |
| 5 | **`records` 筛选参数（§6.4）生效** | 汤 | ✅ | 实调纯度 + `total` 变化 PASS |
| 6 | **后台「违规记录查询」页能看到该条记录** | 吕 | ✅ | e2e §11 经代理查到 `id=8` |
| 7 | **后台「车辆信息管理」页可用（§6.6 CRUD）** | 汤 | ✅ | 四端点 201/409/404/204 全对 |
| 8 | `health`=200，冒烟测试通过 | 吕 | ✅ | e2e §1 PASS |
| 9 | 四规则全过；端点 + 沙箱全过（≥70） | 汤/吕 | ✅ | **132 passed**（超目标 62） |
| 10 | 本周不卡准确率 / 响应时间 | 全员 | ✅ | 遵守，CV 默认 stub |

> **v1.1 修订后第 5、7 项由「⏳」转为「✅」** —— 汤瑾睿在 `24ee309` 补齐，本轮实测确认。

---

## 四、三人任务完成明细

### 4.1 吕浩（统筹 / 集成 / 前端）— 10/10 ✅

| # | 任务 | 状态 | 实测依据 |
|---|---|---|---|
| 1.1 | 前端脚手架 | ✅ | `vite build` exit 0 |
| 1.2 | 接入设计令牌与主题 | ✅ | 副本零漂移 165/165 |
| 1.3 | AppShell + 通用件（8 + 3 + 1） | ✅ | `vue-tsc` + `eslint` exit 0 |
| 1.4 | 侧边导航 6 项（第 6 项置灰） | ✅ | 路由 6 条齐全，含 `/preview` |
| 1.5 | API 客户端 + Vite 代理 | ✅ | e2e §10 代理 200 |
| 1.6 | **第 2 页（Demo 验收项）** | ✅ | 260 行，e2e §11 PASS |
| 1.7 | 第 1 页车辆管理 | ⚠️ **有条件** | 357 行可用，但**仍用 localStorage** |
| 1.8 | 第 3/4/5 页占位路由 | ✅ | 5 个占位视图，路由可达 |
| 1.9 | 联调与 e2e 回归 | ✅ | **12/12 PASS**（含前端 §9–12） |
| 1.10 | 本周集成与合入 | ✅ | `main` 干净，三支已合 |

> **1.7 标注「有条件达标」**：页面功能完整（增删改查 + 二次确认 + 主键只读），但数据源为浏览器 localStorage。契约 §6.6 与后端均已就绪，**属前端未切换**，非接口缺失 —— 已在页面提示条如实标注。

### 4.2 汤瑾睿（后端 / DB / 算法）— 9/9 ✅（附复现局限）

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 2.1 | CV 环境搭建 | ⚠️ **声称完成，待复现** | `check_env.py` 存在；README 载明 Py3.11.9 + torch 2.6.0+cu124 + ultralytics 8.4.155 全 PASS |
| 2.2 | 数据集 ≥120 张 | ⚠️ **声称完成，待复现** | 合成 132 张；**`algo/dataset/` 本机不存在**（gitignore） |
| 2.3 | YOLOv8 训练跑通 | ⚠️ **声称完成，待复现** | `train/detect.py` 存在；mAP50=0.995（**`algo/runs/` 本机不存在**） |
| 2.4 | 车牌识别 + 绿蓝牌判定 | ✅ **代码可核** | `recognize/plate.py` 4787 B，HSV 底色判绿蓝牌 |
| 2.5 | 识别桩 stub/real 开关 | ✅ **已验证** | e2e 默认 stub 正常；环境变量开关，守契约 §7 红线 |
| 2.6 | 规则引擎边界用例 | ✅ | 6 个新用例，含桩不存在/未绑定/阈值 0/时间倒挂 |
| 2.7 | 测试与 lint 全绿 | ✅ | **132 passed**（目标 ≥70）、ruff + black 全过 |
| **2.8** | 🆕 `records` 筛选参数 | ✅ **实测生效** | 纯度 PASS、`total` 随筛选变化 |
| **2.9** | 🆕 车辆 CRUD 四端点 | ✅ **实测通过** | 201/409/404/204；`VehicleUpdate` 类型上不含 plate；删车不级联 |

> **超额产出**：`backend/app/query.py`（共享 `like_ci()` / `enum_exact()`，单一事实源思维）；显式置空 `charging_pile.bound_plate` 以补偿 SQLite 不强制外键。

### 4.3 吴和庆（UI / 美术）— 6/6（3.3/3.4 超额）

| # | 任务 | 状态 | 依据 |
|---|---|---|---|
| 3.1 | 第 2–5 页线框（4 页） | ✅ | `check_tokens.py` 校验 5 页全过 |
| 3.2 | 第 4 页图表配色交付 | ✅ | `component-inventory.md` §3.1 规格 |
| 3.3 | 图标与 Logo（可选） | ✅ **超额** | 16 图标 + 3 Logo |
| 3.4 | 空状态 / 插画（可选） | ✅ **超额** | 2 插画 |
| 3.5 | 校验脚本全绿 | ✅ **超额** | 5 项 → **7 项**；新增反例自测 13 用例 |
| 3.6 | 设计走查（周末） | ⚠️ **有条件达标** | 走查 9/9 自勾；**已由吕浩回签补甲方验收** |

---

## 五、完成比例

### 按任务数

| 成员 | 任务数 | 完成 | 有条件 | 比例 |
|---|---|---|---|---|
| 吕浩 | 10 | 9 | 1（1.7 前端未切换） | **90%** |
| 汤瑾睿 | 9 | 6 | 3（CV 2.1–2.3 待复现） | **67%**（验证维度）／100%（交付维度） |
| 吴和庆 | 6 | 5 | 1（3.6 已补验） | **83%** |
| **合计** | **25** | **20** | **5** | **80%** |

> 说明：汤的 2.1–2.3 属「**交付齐、验证缺**」—— 代码与脚本均在仓库，但**本机无 CV 环境与产物**，无法独立复现。故同一条任务在不同维度下比例不同，不应简单按 100% 计。

### 按里程碑

| 里程碑 | 计划落点 | 状态 |
|---|---|---|
| M5 违规判断规则引擎 | 第 0 天已实现 | ✅ **已完成**（提前） |
| M6 短信提醒（沙箱版） | 第 1 周 | ✅ **已完成**（提前） |
| M7 后台管理平台 | 第 1 周 2 页 | ✅ **2 页可用**（超前于计划） |
| M1 图像采集 | 第 2 周 | ⚠️ 代码就绪，**数据集无凭据** |
| M2 车辆检测 | 第 2 周 | ⚠️ 代码就绪，**权重无凭据** |
| M3 车牌识别 | 第 2 周 | ⚠️ 代码就绪，**准确率未验** |

---

## 六、进行中的工作

| 项 | 状态 | 说明 |
|---|---|---|
| 前端两处切换 | 🔄 **待启动** | 后端已就绪，纯前端任务，替换点已留注释 |
| 契约 v1.4 旧表述清理 | ✅ 已完成 | 8 处「活跃的谎言」已反转（`cb072c6`） |
| tokens 同步机制 | ✅ 已完成 | `copy-tokens.mjs` 三模式，零漂移 165/165 |
| 第 1 周审查文档 | ✅ 已完成 | `WEEK1_REVIEW.md`（吕浩）、`WEEK1_REVIEW_TANG.md`（汤） |

---

## 七、未开始 / 延迟事项

### 7.1 明确延迟（有决策记录）

| 项 | 原定 | 现定 | 原因 |
|---|---|---|---|
| MySQL 8 真机验证 | 第 0 天 | **第 2 周** | 本机未装 MySQL；SQLite 不校验 `ENGINE=InnoDB` / `ENUM` |
| 第 3/4/5 页业务实现 | （契约 §1.1 曾标「做」） | **第 2 周** | 契约 v1.3 已改为「占位路由」，与 PLAN 1.8 对齐 |
| 第 6 页实时预览 | — | **不做** | 导航置灰，预留 |

### 7.2 未开始（计划内，非延迟）

- 前端两处切换（第 2 页筛选传参、第 1 页接 `/api/vehicles`）
- 校园实拍数据集采集与标注
- CV 真机复现（`gen_synth_dataset.py` → `train/detect.py train` → `check_env.py` 存证）
- 第 2 周第 3/4/5 页业务实现

---

## 八、阻塞与风险

### 🔴 R1｜CV 产物无独立凭据（最高风险，关键路径）

| 项 | 内容 |
|---|---|
| **事实** | `algo/` 代码与脚本齐全（`gen_synth_dataset.py` 9901 B、`train/detect.py` 5522 B、`recognize/plate.py` 4787 B、`tests/` 3 文件） |
| **但** | **`algo/dataset/` 与 `algo/runs/` 在本机均不存在**（被 `.gitignore` 第 39–41 行忽略） |
| **且** | `algo/tests` **本机无法运行**：`pytest algo/tests` → **exit 2，2 errors**，`ModuleNotFoundError: No module named 'numpy'`（**硬失败，非跳过**） |
| **后果** | 合成集 132 张、mAP50=0.995、环境 7 项 PASS **均无仓库内凭据**，无法独立复现 |
| **影响** | 本项是 PLAN §1 明写的**全项目唯一真风险**；第 1 周应"启动环境与数据"，现状是**代码启动、证据缺失** |
| **对策** | ① 在有 GPU 的机器（汤本机 RTX 4060）跑三件套并存证到 `docs/acceptance/algo/`；② 给 `algo/tests/conftest.py` 加 `pytest.importorskip("numpy")`，避免无环境时硬报错 |

### 🟠 R2｜合成集不能代表真实准确率

| 项 | 内容 |
|---|---|
| **事实** | 132 张为 `gen_synth_dataset.py` 生成；README 自述"**合成图只能验证流程跑通，不能代表真实准确率**" |
| **后果** | mAP50=0.995 是**合成集内部指标**，第 4 周 S3 性能指标（准确率）**无任何实测支撑** |
| **对策** | 第 2 周必须完成校园实拍（每类 ≥30 张）→ 重标注 → 重训 → 用 `meta.json` 流程回归 |

### 🟡 R3｜前端两处未切换（功能正确性瑕疵）

| 项 | 内容 |
|---|---|
| **事实** | 第 2 页筛选为**前端过滤当前页**（`stores/records.ts` 的 `filteredItems`），第 1 页用 `localStorage` |
| **后果** | **跨页筛选不正确** —— 筛选只作用于当前页数据，翻页会丢失筛选语义。页面上已有 info 条如实告知 |
| **定位** | **纯前端任务**，非后端阻塞；替换点见 `stores/*.ts` 顶部注释 + `frontend/README.md` §6 |
| **对策** | 第 2 周第一天完成 |

### 🟡 R4｜`algo/tests` 缺 skip 守卫

| 项 | 内容 |
|---|---|
| **事实** | 无 `.venv-algo` 时 `import numpy` 硬失败（exit 2），而非 `pytest.importorskip` 跳过 |
| **后果** | 在无 CV 环境的机器上，CI / 本地全量测试会被污染；也掩盖了"未验证"这一事实 |
| **对策** | `algo/tests/conftest.py` 加 `pytest.importorskip("numpy")` + `torch` |

### 🟢 R5｜构建产物体积（已知，非阻塞）

- `index.js` 1,100 kB / gzip 353 kB，`vite build` 提示超 500 kB
- 计划第 4 周打包优化处理，本周不卡

### 🟢 R6｜远程废弃分支对象损坏（管理动作）

- `origin/feature/db-schema` 的 `6f75af67` 对象已损坏 → `git fetch --prune` 整体失败，须定向 fetch
- 建议第 2 周删除该废弃分支

### 🟢 R7｜`algo/README.md` 自评数字过期

- README §3 写"后端 **71** + algo **21**"，实际后端 **132**（71 是 v1.3 两任务之前的旧数）
- 建议随 R1 存证时一并更正

---

## 九、与计划目标的对比

### 9.1 达成情况

| 计划目标 | 实际 | 差异 |
|---|---|---|
| 端到端闭环跑通 | ✅ 12/12 | **达成** |
| 第 1、2 页可操作 | ✅ 2 页可用 | **达成**（第 2 页为 Demo 验收项） |
| 6 页后台骨架 + 导航 | ✅ 6 路由齐 | **达成**（第 6 页置灰） |
| 契约 §11 验收 10/10 | ✅ 10/10 | **达成** |
| CV 环境与数据集就绪 | ⚠️ 代码齐、产物缺 | **部分达成** |
| 测试 ≥70 | ✅ 132 | **超额 89%** |
| check_tokens exit 0 | ✅ exit 0 | **达成** |

### 9.2 提前完成（相对原大纲）

- **M5 规则引擎**：第 0 天即从骨架实现为完整版，并含 4 条边界用例
- **M6 短信沙箱**：第 1 周完成（原计划第 1 周沙箱 → 第 3 周真实 API，节奏正常）
- **M7 后台**：第 1 周 2 页可用（超前，原计划第 1 周为"骨架"）

### 9.3 落后项

| 项 | 落后程度 | 缓冲 |
|---|---|---|
| CV 环境与数据 | **已在关键路径上顺延** | **无缓冲** ⚠️ |
| 校园实拍数据集 | 未开始 | 无 |
| MySQL 8 真机 | 从第 0 天延至第 2 周 | 已纳入第 2 周 |

---

## 十、需要重点关注的行动项

### P0 — 本周内（阻塞第 2 周）

| # | 行动 | 责任 | 验收 |
|---|---|---|---|
| **A1** | **确认汤瑾睿 CV 进度与阻塞点** —— 环境是否真装好？数据是否真生成？为何产物未留凭据？ | **H（管理动作）** | 明确答复 + 时间点 |
| **A2** | 在有 GPU 机器复跑 CV 三件套并存证：`gen_synth_dataset.py` → `train/detect.py train` → `check_env.py`，产物截图/日志放 `docs/acceptance/algo/` | 汤瑾睿 | 仓库内可见凭据 |
| **A3** | `algo/tests/conftest.py` 加 `pytest.importorskip("numpy")` + `torch` | 汤瑾睿 | 无环境时 skip，非 exit 2 |

### P1 — 第 2 周第一天

| # | 行动 | 责任 | 验收 |
|---|---|---|---|
| **B1** | 第 2 页筛选改传参 `/api/records`（服务端筛选 + 分页），移除提示条 | 吕浩 | 跨页筛选正确 |
| **B2** | 第 1 页数据源切 `/api/vehicles`，移除 localStorage 与警示条 | 吕浩 | 刷新后数据持久 |
| **B3** | 校园实拍数据集采集（4 类各 ≥30 张）+ 标注 | 汤瑾睿 | `dataset/` 落盘 |
| **B4** | MySQL 8 真机验证（跑 `schema.sql`，补验 `InnoDB` / `ENUM`） | 汤瑾睿 | 建表成功日志 |
| **B5** | 第 3/4/5 页业务实现（状态展示 / ECharts 统计 / 参数配置） | 吕浩 | 6 页全上线 |

### P2 — 第 2 周内清理

| # | 行动 | 责任 |
|---|---|---|
| **C1** | 更正 `algo/README.md` 自评数字（后端 71 → 132） | 汤瑾睿 |
| **C2** | 删除远程废弃分支 `origin/feature/db-schema`（对象已损坏） | 吕浩 |
| **C3** | 清理本地 tag `backup-before-tang-merge`、备份目录 `C:\tmp\git-backup-damaged` | 吕浩 |
| **C4** | 第 4 周前端打包优化（`index.js` 1.1 MB） | 吕浩 |

---

## 十一、口径与局限声明

1. **本报告全部数据为本轮实测复跑**（后端 5 项、前端 4 项、e2e 12 项、契约实调 4 项），未引用 commit message 或他人自评。
2. **汤瑾睿 2.1–2.3 无法独立复现** —— 本机无 `.venv-algo`，且 `algo/dataset`、`algo/runs` 被 gitignore。按其任务归属标注「**声称完成，待复现**」，不写成"通过"，也不因无法验证而否定其交付。
3. **合成集指标（mAP50=0.995）不等于真实准确率** —— 属合成集内部指标，第 4 周 S3 性能指标需实拍集支撑。
4. **三项工具环境限制**（不影响结论）：本机默认 Python 为 3.13，`.venv`（3.11）需显式指定；`node_modules/.bin` 在沙箱下不生成，须走 `node node_modules/<pkg>/bin/<x>.js`；bash shim 缺 `tail` 等命令。
5. **第 1 周验收判定为 10/10** —— 第 5、7 项由汤瑾睿在合入 `24ee309` 时补齐，本轮实调确认生效。

---

**关联文档**
- `docs/PLAN_4WEEKS.md` v1.1（排期与分工）
- `docs/CONTRACT.md` v1.4（唯一事实源）
- `docs/WEEK1_REVIEW.md`（吕浩第 1 周审查）
- `docs/WEEK1_REVIEW_TANG.md`（汤瑾睿分支审查）
- `docs/acceptance/week1/`（6 页浏览器截图 + §11 逐项对照）
