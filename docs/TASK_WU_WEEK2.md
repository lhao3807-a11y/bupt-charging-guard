# 第 2 周任务书 · 吴和庆（UI / 美术）

> **发放** 吕浩（统筹）· **日期** 2026-09-19 · **对应** `PLAN_4WEEKS.md` §3.3
> **周期** 第 2 周（2026-09-21 ~ 09-27）
> **角色升级** 从「出图」→「**交付可用视觉资产 + 参与实现验收**」

---

## 0. 先看：为什么本周任务变多了

第 1 周你交了 6/6（3.3/3.4 还超额），质量我是认可的。但复盘后发现有**两个结构性问题**：

1. **任务量偏轻** —— 第 1 周 6 项里 2 项标了「可选」，实际有效工时没有吃满
2. **设计与实现之间断档** —— 线框交完就结束了，视觉落地质量**没人把关**：
   - 图标仍是 Element Plus 临时替代（第 1 周允许，现在该收口）
   - 加载态骨架屏你自己在 §5 第 8 项写了「第 2 周补」，一直没人做
   - 答辩素材、品牌收口、a11y 这些**只有你能做**的事，之前没排进计划

所以本周从 6 项 → **14 项**，分 A/B/C 三组。**但优先级是硬的**：

> ### ⏱ 铁律：A 组 > B 组 > C 组
> **A 组未完成，不得动 B 组。** 工期不足时，B/C 可压缩或与 6.5/6.14 合并交付。
> **唯一例外**：6.1（第 6 页线框）无论如何必须完成 —— 它直接进 `check_tokens.py` 第 1 项校验，缺了就红。

---

## A 组：阻塞前端的必做项（优先级最高）

> 这 5 项都直接卡住别人或卡住校验，必须优先。

### 6.1 第 6 页线框（补第 1 周留白）

| 项 | 内容 |
|---|---|
| **为什么** | 第 1 周 §4.6 只做了导航占位，**没有页面线框**。现在 `check_tokens.py` 第 1 项要校验 6 页导航一致性，缺文件就红 |
| **交付物** | `docs/design/wireframes/preview.html` |
| **要求** | ① 照第 1 页风格样板；② 含「最近一帧」展示区 + `el-skeleton` 占位；③ 含 `el-alert`（info）说明条：「仅展示最近一帧截图（轮询刷新），不接实时视频流」；④ 导航 6 项齐全，第 6 项**不再置灰**（本周解禁） |
| **验收** | `check_tokens.py` exit 0（第 1 项导航校验覆盖此页） |
| **依赖** | 无，可立即开工 |

> ⚠️ **注意**：解禁第 6 页导航会影响 `check_tokens.py` 的 `is-reserved` 断言。
> 改导航前先看 `component-inventory.md` §4.6 与 `walkthrough.md` §8，**如需同步改校验脚本，单独 commit 并说明**。

### 6.2 加载态骨架屏规格交付

| 项 | 内容 |
|---|---|
| **为什么** | §5 跨页一致性检查第 8 项自述「加载态是前端实现细节，线框不画骨架屏，**第 2 周补**」—— 现在补，否则吕实现时**无规格可依** |
| **交付物** | `component-inventory.md` 新增 **§2.1 加载态规格** |
| **要求** | 三类骨架：① **表格** `el-skeleton` 行数（建议 8 行）+ 行高 + 列宽对齐策略；② **卡片**（第 3 页桩卡片）3 列 × 2 行规格；③ **图表**（第 4 页 ECharts）容器占位与居中 loading。均给出 `--space-*` 取值 |
| **验收** | 规格中含具体令牌引用（非裸 px）；吕能照此直接实现 |
| **依赖** | 无 |

### 6.3 筛选空结果插画补全

| 项 | 内容 |
|---|---|
| **为什么** | §6.5 自述「`empty-state-search-empty.svg` 留待第 2 周补，本期线框用同一插画 + 不同文案区分」 |
| **交付物** | `docs/design/assets/illustrations/empty-state-search-empty.svg` |
| **要求** | 与现有 2 插画同风格、同尺寸基准（**160×120**）；语义为「筛选无结果」（区别于「暂无数据」「暂无违规记录」）；命名合规；`check_tokens.py` 第 [4] 项素材校验须过 |
| **验收** | 第 [4] 项素材校验通过；3 个插画视觉可区分 |
| **依赖** | 无 |

### 6.4 图标替换实施（临时 → 正式）

| 项 | 内容 |
|---|---|
| **为什么** | 第 1 周 PLAN §2.5 明确允许「第 1 周图标全用 `@element-plus/icons-vue`」，本周该收口了。你已交付 16 个正式图标，但**前端还在用临时图标** |
| **交付物** | ① 前端 `src/assets/icons/` 放入 16 个正式 SVG；② 份替换清单（图标 → 用在哪个组件/页面） |
| **要求** | 逐处替换：导航 6 + 规则 4 + 通用 6。**组件里不再直接引用 Element Plus 图标**（`el-button` 内部自带的除外）。图标取色走 `currentColor` / `--state-*-text`，**不得写死色值** |
| **验收** | 前端 `vue-tsc` + `eslint` + `vite build` 全 exit 0；`check_tokens.py` exit 0；**替换后 e2e 12 项仍全 PASS** |
| **依赖** | 与吕协作 —— **改动单独 commit**，便于出问题时回滚 |
| **⚠️ 风险** | 碰前端代码，可能踩坏现有页面。替换前先 `git pull`，替换后必须重跑前端四件套 |

### 6.5 第 3/4/5 页「实现对齐」复核

| 项 | 内容 |
|---|---|
| **为什么** | 吕第 2 周会把 3/4/5 页从占位做成真实页面。**线框与真实页面的偏差，只有你能识别** |
| **交付物** | 复核记录（追加到 `walkthrough.md` 新章节） |
| **要求** | 逐页比对：间距（是否 4px 栅格）、色档（白底是否用 `-text` 档）、字号、空态、等宽字族、状态色映射（rule_hit 1→红/2→橙/3→黄/0→绿） |
| **验收** | **列出偏差清单与修改建议，不直接改代码**（改代码是吕的事，避免双方同时改冲突） |
| **依赖** | **依赖吕完成 4.2/4.3/4.4** → 排在周中后 |

---

## B 组：视觉资产与演示素材（新增）

> 这些是**只有你能做、且答辩必用**的东西。A 组完成后启动。

### 6.6 系统登录页设计 + 实现

| 项 | 内容 |
|---|---|
| **为什么** | **答辩第一眼看到的就是登录页**，目前完全没有 |
| **交付物** | 线框 `wireframes/login.html` + 前端可直接用的页面结构 |
| **要求** | 用 `logo-full.svg`（完整版）；含用户名/密码输入 + 错误态 + 「记住我」；居中卡片布局；**含 loading 态与错误提示态** |
| **验收** | 遵循 `tokens.css`，无裸色值；与 `logo-full.svg` 的副标题文案一致 |

### 6.7 全站品牌视觉收口

| 项 | 内容 |
|---|---|
| **交付物** | ① 侧边栏换 `logo-mark.svg`（**替换当前线框里的「桩」字占位**）；② 页脚版权行设计；③ `favicon`（基于 `logo-mark`，产出 `.ico` 或 `.svg`） |
| **要求** | 侧边栏容器 28×28；`favicon` 需在前端 `index.html` 可引用 |
| **验收** | 6 页侧边栏视觉一致；浏览器标签页显示品牌图标 |

### 6.8 设计系统深色模式方案

| 项 | 内容 |
|---|---|
| **交付物** | `design-tokens.md` 新增章节 + 样张 |
| **要求** | 给出 `--el-*` 深色覆盖方案 + **对比度验算表**（复用现有 3 档对比度口径）；给 1–2 页样张 |
| **验收** | ⚠️ **只出方案不实施** —— 第 4 周视工期决定是否实施。**别自己动手改 tokens.css** |

### 6.9 数据可视化视觉规范

| 项 | 内容 |
|---|---|
| **为什么** | §3.1 已有「图表规格」，但吕要用 ECharts 落地，需要**可逐条对照**的主题规范 |
| **交付物** | `component-inventory.md` 新增 **§3.2 ECharts 主题规范** |
| **要求** | 色板序列（danger 红 → caution 橙 → warning 黄 → success 绿）、网格线、坐标轴、tooltip、图例样式、**无数据态**。全部用 `var(--token)` 表述 |
| **验收** | **与吕的第 4 页实现逐条对齐**（吕实现后你复核，或你先给规范吕照做） |

### 6.10 答辩演示素材（第一版）

| 项 | 内容 |
|---|---|
| **交付物** | `docs/design/demo/` 下三样 |
| **要求** | ① **6 页系统截图美化**（统一设备框、窗口标题、脱敏处理）；② **演示流程图**（识别→判定→提醒→落库，含架构分层）；③ **答辩 PPT 视觉模板**（封面 + 目录 + 内容 + 结束页） |
| **验收** | 截图须**真实页面**（不许用线框冒充），可配合吕用 `docs/acceptance/week1/README.md` 的截图方法 |
| **⚠️ 数据脱敏** | 手机号必须脱敏（`138****6621`，已确认口径）；**车牌是否需要脱敏请先问吕** |

---

## C 组：质量与规范（新增）

### 6.11 界面文案规范

| 项 | 内容 |
|---|---|
| **交付物** | `docs/design/copywriting.md` |
| **要求** | ① 统一术语（**「车牌」vs「车牌号」按契约 §3.5 已定口径**，别再纠结）；② 错误提示写法；③ 确认文案（删除二次确认等）；④ 空态文案 |
| **验收** | **须与前端现有文案逐条核对**，发现不一致列出清单 |

### 6.12 `check_tokens.py` 新增断言（≥2 条）

| 项 | 内容 |
|---|---|
| **交付物** | `tools/check_tokens.py` + `selftest_check_tokens.py` 各新增 |
| **建议方向** | ① 加载态骨架屏类名断言；② 第 6 页线框存在性断言；③ `demo/` 素材 SVG 可解析断言 |
| **要求** | 🔴 **每条新断言必须配反例打靶** —— 故意改坏，看是否 exit 1 |
| **验收** | `check_tokens.py` exit 0；`selftest` 从 13 例 → **≥15 例且全拦下** |
| **⚠️ 教训** | 你第 1 周的 `selftest` 做得很好（13/13）。**但这次的坑是「白名单挡不住自己写错自己」** —— 第 2 页写「车牌号」而第 4 页写「车牌」就是这么漏的。新断言要避免同类问题 |

### 6.13 可访问性（a11y）基础检查

| 项 | 内容 |
|---|---|
| **交付物** | 检查报告（可并入 `walkthrough.md`） |
| **要求** | ① 键盘可达性（Tab 序是否合理）；② `aria-label` 覆盖（**仅图标按钮**必查）；③ 对比度复核 |
| **验收** | **列出问题清单与修复建议，不直接改代码** |

### 6.14 设计走查（本周第二轮）

| 项 | 内容 |
|---|---|
| **交付物** | `walkthrough.md` 追加 §9 |
| **要求** | 对照 §5 检查单 9 项 + 本轮新增项；**必须由吕浩回签** |
| **验收** | ⚠️ **不能自己评自己** —— 第 1 周你的走查 9/9 全勾但无甲方验收，我那边是补签的。这周请**主动把需要我确认的项单列出来** |

---

## 交付节奏建议

| 时点 | 应完成 |
|---|---|
| **周一–周二** | 6.1（第 6 页线框）、6.2（加载态规格）、6.3（空结果插画） |
| **周三** | 6.4（图标替换，与吕同步）、启动 6.6 / 6.7 |
| **周四** | 6.9（可视化规范，与吕对齐）、6.8（深色方案） |
| **周五** | 6.5（实现对齐复核）、6.12（新断言 + 打靶）、6.13（a11y） |
| **周末** | 6.10（答辩素材）、6.11（文案规范）、6.14（走查 + 待回签项） |

> 以上是建议节奏，**A 组完成时间可前压**。周五同步会请带：**已完成 / 卡点 / 下周需要什么**。

---

## 三条红线（第 1 周已立，本周继续）

1. **页面里不出现裸色值** —— 一律 `var(--token)`。白底上可见的用 `-text` 档，`-solid` 只能当填充块
2. **表格列名以契约 §3.5 为准** —— 唯一事实源。想加字段，去改契约，别绕过校验
3. **交付前必跑** `python docs/design/tools/check_tokens.py`（**exit 0**）+ `selftest_check_tokens.py`（全拦下）

> 提醒：`docs/design/tools/**` **不在项目 lint 范围**（标准命令是 `ruff check backend scripts`），
> 所以你那两个脚本的 `UP031` 风格问题**不算你的锅**，别顺手格式化它们。

---

## 附：本周你新增的契约前置依赖

第 3/4/5 页各依赖一个**尚未定义的接口**，汤会在周一定义（契约 C1–C3）：

| 页 | 依赖接口 | 影响你的什么 |
|---|---|---|
| 第 3 页 | `GET /api/piles` | 线框已交付，**若接口字段与线框不符，需你确认以契约为准** |
| 第 4 页 | `GET /api/stats` | §3.2 可视化规范需按**实际返回结构**设计序列 |
| 第 5 页 | `GET/PUT /api/config` | 线框已交付，仅 2 个阈值 |

**所以：6.9（可视化规范）建议等契约 C2 定义后再做**，否则可能白做。

---

**关联文档**
- `docs/PLAN_4WEEKS.md` §3.3（本任务书出处）
- `docs/design/component-inventory.md`（组件与素材规格）
- `docs/design/walkthrough.md`（走查记录，§8 为吕浩回签）
- `docs/CONTRACT.md` v1.4（唯一事实源）
- `docs/WEEK1_PROGRESS.md`（第 1 周实测复盘）
