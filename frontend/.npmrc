# 国内镜像加速（2026-09-19）
#
# 背景：直连 registry.npmjs.org 在校园网/沙箱环境下极慢或不可达。
# 注意：**清华 TUNA 的 npm 镜像已停用**（`https://mirrors.tuna.tsinghua.edu.cn/npm/`
#       实测返回 404），不要再用。以下两个实测可用：
#         - https://mirrors.cloud.tencent.com/npm/   （腾讯云，本项目采用）
#         - https://registry.npmmirror.com           （阿里 npmmirror，备选）
registry=https://mirrors.cloud.tencent.com/npm/

# 二进制依赖的下载源（node-sass / electron / puppeteer 等）
# 本项目暂未用到，预留以防后续引入需要编译的包
# sass_binary_site=https://mirrors.cloud.tencent.com/npm/node-sass
# ELECTRON_MIRROR=https://mirrors.cloud.tencent.com/npm/electron/

# 关掉 audit / fund 减少无谓请求
audit=false
fund=false
